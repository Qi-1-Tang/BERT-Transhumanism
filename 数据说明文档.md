# 数据文件说明文档

本文档详细说明项目中所有数据文件的用途、格式、字段含义和使用方法，帮助理解每个数据文件的作用和内容。

**文档更新说明**：
- **更新内容**：根据更新的语料库（20本中方网文 + 200本西方科幻）重新进行了数据清洗、有监督分类和无监督聚类分析
- **数据规模变化**：
  - 总记录数：从161,724条增加到436,934条
  - 中方书籍：从10本增加到20本
  - 西方书籍：从20本增加到200本
  - 类别比例：从不平衡（12.5:1）改善为更平衡（2.17:1）

**路径说明**：
- 文档中的文件路径以`/root/autodl-tmp/clean/`开头，这是服务器上的实际路径
- 如果在本地环境使用，请根据实际情况调整路径
- 相对路径：所有路径都是相对于项目根目录（`clean/`）的

---

## 目录

1. [训练数据文件](#1-训练数据文件)
2. [数据清洗报告文件](#2-数据清洗报告文件)
3. [模型训练输出文件](#3-模型训练输出文件)
4. [主题分析数据文件](#4-主题分析数据文件)
5. [原始文本文件](#5-原始文本文件)
6. [模型配置文件](#6-模型配置文件)
7. [数据文件使用指南](#7-数据文件使用指南)

---

## 1. 训练数据文件

### 1.1 `bert_training_dataset.csv`

**文件路径**：`/root/autodl-tmp/clean/bert_training_dataset.csv`


**文件用途**：
- 这是BERT模型训练的主要数据集
- 包含所有预处理后的文本片段及其标签
- 用于有监督分类模型的训练和验证

**文件格式**：CSV（逗号分隔值）

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `text` | String | 文本内容（已清洗的文本片段） | "The starship drifted through the void..." |
| `source` | String | 文本来源类别 | "Western_SciFi" 或 "Chinese_Xianxia" |
| `book_name` | String | 来源书籍名称 | "Dune" 或 "Lord of the Mysteries" |
| `chunk_id` | Integer | 文本片段在书籍中的编号 | 1, 2, 3, ... |
| `char_count` | Integer | 文本字符数 | 1200 |
| `estimated_tokens` | Integer | 估计的Token数量（用于BERT） | 300 |
| `Timestamps` | String | 文本对应的时间戳（书籍发表/完结/获奖年份） | "2016" |

**数据统计**：
- **总记录数**：436,934条
- **Western_SciFi（西方科幻）**：137,645条（31.5%）
- **Chinese_Xianxia（中方网文）**：299,289条（68.5%）
- **平均字符数**：~1,147字符
- **平均Token数**：~287 tokens

**数据来源**：
- 来自220本小说（20本中方网文 + 200本西方科幻）
- 经过文本清洗和切分处理
- 使用滑动窗口切分（窗口大小1200字符，重叠150字符）


**注意事项**：
- 文件较大，读取时建议使用分块读取（`chunksize`参数）
- 文本已进行清洗，移除了垃圾内容和格式标记
- 每个文本片段长度控制在BERT的512 token限制内

---

## 2. 数据清洗报告文件

### 2.1 `clean_check_report.csv`

**文件路径**：`/root/autodl-tmp/clean/clean_check_report.csv`

**文件用途**：
- 记录数据清洗过程中检测到的所有垃圾内容
- 用于质量控制和清洗效果验证
- 帮助理解数据清洗的详细过程

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `文件夹` | String | 文件所在文件夹 | "Cleaned_Chinese_Trans_V3" |
| `文件名` | String | 源文件名 | "clean_My House of Horrors.txt" |
| `行号` | Integer | 垃圾内容所在行号 | 14 |
| `内容` | String | 被标记为垃圾的内容 | "..." |
| `垃圾类型` | String | 垃圾内容的分类 | "纯分割线" |

**垃圾类型说明**：
- **纯分割线**：仅包含分隔符的行（如"..."、"---"等）
- **翻译人员名单**：包含译者、编辑等信息的行
- **NovelFull水印**：网站水印文本
- **URL删除**：包含网址的行
- **其他**：其他类型的垃圾内容


---

### 2.2 `deleted_report_cn_v3.csv`

**文件路径**：`/root/autodl-tmp/clean/deleted_report_cn_v3.csv`

**文件用途**：
- 记录中方网文（Chinese_Xianxia）清洗过程中删除的内容
- 详细记录删除原因和位置

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `文件名` | String | 源文件名 | "Library of Heaven is Path.txt" |
| `行号` | Integer | 删除内容所在行号 | 10 |
| `原内容` | String | 被删除的原始内容 | "Translator: StarveCleric Editor: Thaddpo" |
| `原因` | String | 删除原因 | "翻译人员名单" |

**删除原因类型**：
- **翻译人员名单**：译者、编辑信息
- **NovelFull水印**：网站水印
- **URL删除**：包含网址的内容
- **纯分割线**：仅分隔符的行
- **其他**：其他需要删除的内容


---

### 2.3 `deleted_report_v2.csv`

**文件路径**：`/root/autodl-tmp/clean/deleted_report_v2.csv`

**文件用途**：
- 记录西方科幻（Western_SciFi）清洗过程中删除的内容
- 详细记录删除原因和位置

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `文件名` | String | 源文件名 | "Blindsight.txt" |
| `行号` | Integer | 删除内容所在行号 | 7574 |
| `内容` | String | 被删除的原始内容 | "www.feedbooks.com" |
| `原因` | String | 删除原因 | "包含网址 (URL)" |

**删除原因类型**：
- **包含网址 (URL)**：网站链接
- **目录导航**：目录索引内容
- **长句高频重复**：重复出现的句子（如重复26次）
- **其他**：其他需要删除的内容


---

### 2.4 `deep_clean_report.csv`

**文件路径**：`/root/autodl-tmp/clean/deep_clean_report.csv`

**文件用途**：
- 记录深度数据清洗过程中检测到的所有垃圾内容
- 用于质量控制和清洗效果验证
- 包含更详细的清洗信息（针对更新后的语料库）

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `来源文件夹` | String | 文件所在文件夹 | "Cleaned_English_V2" |
| `文件名` | String | 源文件名 | "clean_Powers.txt" |
| `行号` | Integer | 垃圾内容所在行号 | 561 |
| `原内容` | String | 被标记为垃圾的原始内容 | "♦ ♦ ♦" |
| `原因` | String | 垃圾内容的分类 | "纯符号/分割线" |

**垃圾类型说明**：
- **纯符号/分割线**：仅包含分隔符的行（如"♦ ♦ ♦"、"..."、"---"等）
- **翻译人员名单**：包含译者、编辑等信息的行
- **网站水印**：网站水印文本
- **URL删除**：包含网址的行
- **其他**：其他类型的垃圾内容

**数据说明**：
- 此报告针对更新后的语料库（20本中方 + 200本西方）进行了深度清洗
- 清洗范围更广，检测更细致
- 确保数据质量达到训练要求

---

## 3. 模型训练输出文件

### 3.1 模型文件

#### `best_model_epoch_X.pt`

**文件路径**：
- `training_output/models/best_model_epoch_1.pt(未保存)`
- `training_output/models/best_model_epoch_2.pt(未保存)`
- `training_output/models/best_model_epoch_3.pt(未保存)`
- `training_output_stage2_classweights/models/best_model_epoch_1.pt(未保存)`
- `training_output_stage2_classweights/models/best_model_epoch_2.pt(未保存)`
- `training_output_stage2_classweights/models/best_model_epoch_3.pt(未保存)`
- `training_output_stage2_classweights_focal/models/best_model_epoch_3.pt`（**最新模型**）

**文件用途**：
- 保存训练好的BERT分类模型
- 包含模型权重、优化器状态、训练配置等信息
- 用于后续的文本分类预测

**文件格式**：PyTorch模型文件（.pt）

**文件内容**：
- `model_state_dict`：模型权重参数
- `optimizer_state_dict`：优化器状态
- `epoch`：训练轮数
- `best_f1`：最佳F1-Score
- `config`：训练配置参数

**推荐使用**：
- **最佳模型**：`training_output_stage2_classweights_focal/models/best_model_epoch_3.pt`
- **性能指标**：准确率99.99%，F1-Score 99.99%（Macro F1 99.99%）


---

### 3.2 评估报告文件

#### `classification_report_*.txt`

**文件路径**：
- `training_output/reports/classification_report_best_model_epoch_3.txt`
- `training_output_stage2_classweights/reports/classification_report_best_model_epoch_3.txt`
- `training_output_stage2_classweights_focal/reports/classification_report_stage2_classweights_focal.txt`

**文件用途**：
- 详细的分类性能报告
- 包含每个类别的精确率、召回率、F1-Score等指标

**文件格式**：纯文本（TXT）

**文件内容示例**：
```
              Precision    Recall    F1-Score    Support
Western_SciFi        0.9998     0.9999     0.9998     27529
Chinese_Xianxia      0.9999     0.9999     0.9999     59858
准确率 (Accuracy)                             0.9999     87387
```

**指标说明**：
- **precision（精确率）**：预测为正类的样本中，真正为正类的比例
- **recall（召回率）**：真正为正类的样本中，被正确预测的比例
- **f1-score**：精确率和召回率的调和平均
- **support**：该类别在验证集中的样本数量

---

#### `metrics_*.json`

**文件路径**：
- `training_output/reports/metrics_best_model_epoch_3.json`
- `training_output_stage2_classweights/reports/metrics_stage2_classweights.json`
- `training_output_stage2_classweights/reports/metrics_best_model_epoch_3.json`
- `training_output_stage2_classweights_focal/reports/metrics_stage2_classweights_focal.json`

**文件用途**：
- 以JSON格式保存所有评估指标
- 便于程序化读取和分析

**文件格式**：JSON

**文件内容结构**：
```json
{
  "overall_accuracy": 0.9998970098527241,
  "roc_auc": 0.9999996783650521,
  "pr_auc": 0.9999998524443298,
  "macro_f1": 0.999880682558935,
  "weighted_f1": 0.9998970113679714,
  "class_0": {
    "precision": 1.0,
    "recall": 0.9998910240110429,
    "f1": 0.9999455090364181
  },
  "class_1": {
    "precision": 1.0,
    "recall": 0.9998997627718935,
    "f1": 0.9999498788739454
  },
  "confusion_matrix": [[27526, 3], [6, 59852]],
  "avg_confidence": 0.9989607334136963,
  "val_loss": 0.0004991283183475088,
  "total_training_time": 13424.593098402023
}
```

**字段说明**：
- `overall_accuracy`：总体准确率
- `roc_auc`：ROC曲线下面积
- `pr_auc`：PR曲线下面积
- `macro_f1`：宏平均F1-Score
- `weighted_f1`：加权平均F1-Score
- `class_0`：Western_SciFi类别的指标
- `class_1`：Chinese_Xianxia类别的指标
- `confusion_matrix`：混淆矩阵
- `avg_confidence`：平均预测置信度
- `val_loss`：验证集损失

---

#### `metrics_summary.json`

**文件路径**：
- `training_output/reports/metrics_summary.json`
- `training_output_stage2_classweights/reports/metrics_summary.json`

**文件用途**：
- 汇总所有训练轮次（epoch）的评估指标
- 便于对比不同轮次的性能

**文件格式**：JSON数组

**文件内容结构**：
```json
[
  {
    "epoch": 1,
    "train_loss": 0.0021,
    "val_loss": 0.0018,
    "val_f1_macro": 0.9983,
    "val_f1_weighted": 0.9985
  },
  {
    "epoch": 2,
    "train_loss": 0.0003,
    "val_loss": 0.0005,
    "val_f1_macro": 0.9998,
    "val_f1_weighted": 0.9998
  },
  {
    "epoch": 3,
    "train_loss": 0.0001,
    "val_loss": 0.0005,
    "val_f1_macro": 0.9999,
    "val_f1_weighted": 0.9999
  }
]
```


---

#### `confusion_matrix_*.png`

**文件路径**：
- `training_output/reports/confusion_matrix_best_model_epoch_3.png`
- `training_output_stage2_classweights/reports/confusion_matrix_best_model_epoch_3.png`
- `training_output_stage2_classweights_focal/reports/confusion_matrix_stage2_classweights_focal.png`

**文件用途**：
- 混淆矩阵可视化图像
- 直观展示模型在每个类别上的预测准确性

**文件格式**：PNG图像文件

**图像内容**：
- 横轴：预测标签（Predicted Label）
- 纵轴：真实标签（True Label）
- 矩阵数值：每个类别的预测数量
- 颜色深浅：表示数量多少

**解读方法**：
- 对角线上的数值越大越好（表示正确预测）
- 非对角线上的数值越小越好（表示错误预测）

---

#### `roc_pr_curves_*.png`

**文件路径**：
- `training_output/reports/roc_pr_curves_best_model_epoch_3.png`
- `training_output_stage2_classweights/reports/roc_pr_curves_best_model_epoch_3.png`
- `training_output_stage2_classweights_focal/reports/roc_pr_curves_stage2_classweights_focal.png`

**文件用途**：
- ROC曲线和PR曲线的可视化图像
- 评估模型的分类性能

**文件格式**：PNG图像文件

**图像内容**：
- **ROC曲线**：展示真正率（TPR）与假正率（FPR）的关系
- **PR曲线**：展示精确率（Precision）与召回率（Recall）的关系
- AUC值：曲线下面积，越接近1.0越好

**解读方法**：
- ROC AUC接近1.0表示模型性能优秀
- PR AUC接近1.0表示模型在正类预测上表现优秀

---

#### `training_curves_*.png`

**文件路径**：
- `training_output_stage2_classweights/reports/training_curves_stage2_classweights.png`
- `training_output_stage2_classweights_focal/reports/training_curves_stage2_classweights_focal.png`

**文件用途**：
- 训练过程曲线图
- 展示训练损失和验证损失随训练轮次的变化

**文件格式**：PNG图像文件

**图像内容**：
- 训练损失曲线（Training Loss）
- 验证损失曲线（Validation Loss）
- 横轴：训练轮次（Epoch）
- 纵轴：损失值（Loss）

**解读方法**：
- 损失值应该随训练轮次逐渐下降
- 训练损失和验证损失应该保持接近，避免过拟合

---

#### `error_samples_*.csv`

**文件路径**：
- `training_output_stage2_classweights/reports/error_samples_stage2_classweights.csv`
- `training_output_stage2_classweights_focal/reports/error_samples_stage2_classweights_focal.csv`（**最新**）

**文件用途**：
- 记录模型预测错误的样本
- 用于分析模型的错误模式和改进方向

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `text` | String | 文本内容 | "My case didn't really interest him..." |
| `true_label` | String | 真实标签 | "Western_SciFi" |
| `predicted_label` | String | 预测标签 | "Chinese_Xianxia" |
| `confidence` | Float | 预测置信度 | 0.9998 |
| `prob_class_0` | Float | 类别0的概率 | 0.0001 |
| `prob_class_1` | Float | 类别1的概率 | 0.9999 |

**数据说明**：
- 仅包含预测错误的样本
- 置信度通常很高（接近1.0），说明模型对错误预测也很"自信"
- 可用于分析边界案例和模型局限性
- 更新后的模型共有9个错误样本（3个Western_SciFi→Chinese_Xianxia，6个Chinese_Xianxia→Western_SciFi）


---

## 4. 主题分析数据文件

本部分包含使用BERTopic进行无监督主题建模和情感分析生成的所有数据文件。

> **注意**：以下数据基于更新后的语料库（20本中方 + 200本西方，共436,934条文档）重新进行了无监督聚类分析。

---

### 4.1 `topic_analysis.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis.csv`

**文件用途**：
- 主题建模的主要结果文件
- 包含每个主题的关键词、频率、分布等信息
- 用于快速了解文本数据中的主要主题

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `topic_id` | Integer | 主题ID（-1表示噪声/未分类） | 0, 1, 2, ... |
| `topic_keywords` | String | 主题关键词（逗号分隔） | "experts, space, skeleton, mental, origin..." |
| `topic_keywords_with_scores` | String | 带c-TF-IDF分数的关键词 | "experts(0.2247); space(0.2234); skeleton(0.2122)..." |
| `frequency` | Integer | 该主题的文档数量 | 26444 |
| `percentage` | Float | 该主题的文档占比（%） | 6.05 |
| `source_distribution` | String | 来源分布（各类别的文档数） | "Chinese_Xianxia: 26235, Western_SciFi: 209" |
| `files` | String | 包含该主题的文件列表 | "clean_Swallowed Star.txt, clean_Foundation Trilogy.txt..." |

**数据说明**：
- **topic_id = -1**：表示噪声主题，包含无法归类到任何主题的文档
- **topic_id ≥ 0**：表示有效主题，按频率从高到低排序
- **关键词分数**：c-TF-IDF分数越高，该词对主题的代表性越强

**数据统计**：
- 总主题数：159个（158个有效主题 + 1个噪声主题）
- 总文档数：436,934个
- 最大主题（topic_id=0）：26,444个文档（6.05%）
- 噪声主题（topic_id=-1）：55,689个文档（12.75%）
- 有效主题文档数：381,245个（87.25%）
- 最小有效主题（topic_id=157）：171个文档（0.04%）


**注意事项**：
- 与旧版（49个主题，噪声49.16%）相比，更新后主题数量大幅增加（158个），噪声比例显著降低（12.75%），说明更大规模的语料库提供了更丰富的主题信号
- 关键词可以帮助理解每个主题的内容特征
- source_distribution字段可以分析主题在不同类别中的分布

---

### 4.2 `topic_analysis_detailed_topics.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_detailed_topics.csv`

**文件用途**：
- 主题的详细信息，包括代表性文档
- 用于深入理解每个主题的具体内容
- 包含每个主题的代表性文档片段

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `Topic` | Integer | 主题ID | 0 |
| `Count` | Integer | 文档数量 | 26444 |
| `Name` | String | 主题名称（基于关键词） | "0_experts_space_skeleton_mental" |
| `Representation` | String | 主题表示（关键词列表） | "['experts', 'space', 'skeleton', 'mental'...]" |
| `Representative_Docs` | String | 代表性文档（JSON格式） | "['文档1...', '文档2...', '文档3...']" |

**数据说明**：
- **Representative_Docs**：包含最能代表该主题的文档片段（通常是3个）
- 这些文档可以帮助理解主题的实际内容
- 文档以JSON数组格式存储


---

### 4.3 `topic_analysis_document_topics.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_document_topics.csv`

**文件大小**：~620MB（包含所有文档的完整文本）

**文件用途**：
- 每个文档的主题分配表
- 记录每个文档被分配到哪个主题
- 包含文档的完整文本（用于后续情感分析）

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `document_id` | Integer | 文档ID（从0开始） | 0, 1, 2, ... |
| `text_preview` | String | 文本预览（前200字符） | "Before they left, Exquisite told..." |
| `text` | String | 完整文本内容 | "完整的文档文本..." |
| `topic_id` | Integer | 分配的主题ID | 0, 1, 2, -1 |
| `topic_probability` | Float | 主题分配概率（可选） | 0.85 |
| `source_file` | String | 来源文件名 | "clean_Swallowed Star.txt" |
| `source` | String | 来源类别（可选） | "Chinese_Xianxia" |
| `timestamp` | String | 时间戳（书籍发表/完结/获奖年份） | "2016" |

**数据说明**：
- 每个文档都会被分配到一个主题（包括噪声主题-1）
- `topic_probability`表示文档属于该主题的置信度
- `text`字段包含完整文本，文件较大


**注意事项**：
- 文件非常大（>200MB），包含所有文档的完整文本
- 读取时建议使用`nrows`参数限制行数，或使用分块读取
- 这是情感分析的输入文件

---

### 4.4 `topic_sentiment_final.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_sentiment_final.csv`

**文件用途**：
- 每个主题的情感分析统计结果
- 结合主题建模和情感分析的综合结果
- 用于分析不同主题的情感倾向

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `Topic` | Integer | 主题ID | 0 |
| `Sentiment_Mean` | Float | 平均情感得分（-1到1） | -0.1983 |
| `Sentiment_Std` | Float | 情感得分标准差 | 0.3378 |
| `Frequency` | Integer | 该主题的文档数量 | 26444 |
| `Positive_Count` | Integer | 积极情感文档数 | 6660 |
| `Negative_Count` | Integer | 消极情感文档数 | 19784 |
| `Neutral_Count` | Integer | 中性情感文档数 | 0 |
| `Keywords` | String | 主题关键词标识 | "Topic 0" |

**情感得分说明**：
- **Sentiment_Mean > 0**：整体情感倾向为积极
- **Sentiment_Mean < 0**：整体情感倾向为消极
- **Sentiment_Mean ≈ 0**：整体情感倾向为中性
- **Sentiment_Std**：标准差越大，说明该主题内情感分布越分散

**情感分类标准**：
- **Positive_Count**：情感得分 > 0 的文档数
- **Negative_Count**：情感得分 < 0 的文档数
- **Neutral_Count**：情感得分 = 0 的文档数（通常为0）

**数据解读示例**：
- Topic 0: 平均情感-0.198（略消极），6,660个积极文档，19,784个消极文档
- 说明该主题整体偏向消极情感，但内部有较大差异


---

### 4.5 `temp_sentiment_docs.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/temp_sentiment_docs.csv`

**文件大小**：>200MB（包含所有文档的情感得分）

**文件用途**：
- 文档级情感得分的中间结果文件
- 记录每个文档的情感得分（-1到1之间）
- 用于生成主题级情感统计

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `document_id` | Integer | 文档ID | 0, 1, 2, ... |
| `text` | String | 文档文本 | "完整的文档文本..." |
| `sentiment_score` | Float | 情感得分（-1到1） | -0.2345 |
| `topic_id` | Integer | 主题ID | 0 |
| `source_file` | String | 来源文件名 | "clean_Swallowed Star.txt" |

**情感得分说明**：
- **-1.0**：极端消极
- **0.0**：中性
- **1.0**：极端积极
- 使用RoBERTa-base-sentiment模型计算

**注意事项**：
- 这是中间结果文件，通常不保存（`SAVE_TEMP_FILE = False`）
- **当前项目中此文件不存在**，因为默认配置未启用保存
- 如果需要此文件，可以在运行情感分析时设置 `SAVE_TEMP_FILE = True`
- 文件很大，包含所有文档的完整文本


---

### 4.6 主题可视化文件

#### `topic_barchart.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/topic_barchart.html`

**文件用途**：
- 主题频率条形图（交互式）
- 可视化每个主题的文档数量和关键词

**文件格式**：HTML（使用Plotly生成）

**可视化内容**：
- 横轴：主题ID
- 纵轴：文档数量
- 每个条形显示主题的关键词
- 支持交互式操作（缩放、悬停查看详情）

**使用方法**：
- 直接在浏览器中打开HTML文件
- 可以悬停查看详细信息
- 可以缩放和筛选

---

#### `topic_distance.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/topic_distance.html`

**文件用途**：
- 主题距离可视化（降维后的2D映射图）
- 展示主题之间的相似性和关系

**文件格式**：HTML（交互式）

**可视化内容**：
- 每个圆圈代表一个主题，圆圈大小代表主题频率
- 距离越近的主题越相似
- 交互式图表，支持缩放和悬停查看详情
- 可以点击查看主题详情

**解读方法**：
- 聚集在一起的主题可能相关
- 孤立的主题可能是独特的主题
- 噪声主题（-1）通常远离其他主题

---

#### `topic_hierarchy.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/topic_hierarchy.html`

**文件用途**：
- 主题层次结构可视化
- 展示主题之间的层级关系

**文件格式**：HTML（交互式）

**可视化内容**：
- 树状结构展示主题层次
- 可以展开/折叠查看不同层级
- 帮助理解主题的组织结构

---

#### `ctfidf_scores.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/ctfidf_scores.html`

**文件用途**：
- c-TF-IDF分数可视化
- 展示每个主题中关键词的重要性

**文件格式**：HTML（交互式）

**可视化内容**：
- 每个主题的关键词及其c-TF-IDF分数
- 分数越高，关键词对主题的代表性越强
- 可以按主题筛选查看

---

#### `topic_similarity_matrix.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/topic_similarity_matrix.html`

**文件用途**：
- 主题相似度矩阵可视化
- 展示不同主题之间的相似程度

**文件格式**：HTML（交互式Plotly图表）

**可视化内容**：
- 热力图形式展示主题间的余弦相似度
- 颜色越深表示两个主题越相似
- 可用于发现可合并的相似主题

---

#### `topic_comparison_bar.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/topic_comparison_bar.html`

**文件用途**：
- 主题在不同来源（中方/西方）中的分布对比
- 直观展示每个主题的来源组成

**文件格式**：HTML（交互式Plotly图表）

**可视化内容**：
- 堆叠条形图展示主题的来源分布
- 区分Chinese_Xianxia和Western_SciFi的贡献
- 可用于分析哪些主题是某一类别特有的

---

#### `topic_comparison_data.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/topic_comparison_data.csv`

**文件用途**：
- 主题来源对比的原始数据文件
- 包含每个主题在不同来源（中方/西方）中的文档数量
- 是 `topic_comparison_bar.html` 可视化图表的数据来源

**文件格式**：CSV

---

#### `documents_and_topics.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_output/documents_and_topics.html`

**文件用途**：
- 文档-主题分布的交互式可视化
- 展示文档在主题空间中的分布

**文件格式**：HTML（交互式Plotly图表）

---

#### `topics_over_time.html` / `topics_over_time.csv`

**文件路径**：
- `bertopic_analysis/topic_analysis_output/topics_over_time.html`
- `bertopic_analysis/topic_analysis_output/topics_over_time.csv`

**文件用途**：
- 展示主题随时间（作品发表/完结/获奖时间）的变化趋势
- 分析不同时期的主题热度演变

**文件格式**：HTML（交互式图表）+ CSV（原始数据）

---

#### `sentiment_quadrant_chart.html`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/sentiment_quadrant_chart.html`

**文件用途**：
- 情感-主题象限图
- 同时展示主题的频率和情感倾向

**文件格式**：HTML（交互式Plotly图表）

**可视化内容**：
- **横轴**：主题频率（文档数量）
- **纵轴**：平均情感得分（-1到1）
- **四个象限**：
  - 右上：高频+积极情感
  - 右下：高频+消极情感
  - 左上：低频+积极情感
  - 左下：低频+消极情感
- 每个点代表一个主题，大小表示文档数量

**解读方法**：
- 右上象限：重要且积极的主题
- 右下象限：重要但消极的主题
- 可以点击查看主题详情

**使用方法**：
- 直接在浏览器中打开
- 悬停查看主题详细信息
- 可以缩放和筛选

---

## 5. 原始文本文件

### 5.1 清洗后的文本文件

清洗后的文本文件是经过数据清洗处理后的纯净小说正文，已移除所有垃圾内容（如翻译人员名单、网站水印、URL链接、纯分割线等）。这些文件是训练数据集和主题分析的直接数据来源。

---

#### 5.1.1 `Cleaned_Chinese_Trans_V3/` 目录

**文件路径**：`/root/autodl-tmp/clean/Cleaned_Chinese_Trans_V3/`

**目录用途**：
- 存储中方网文（Chinese_Xianxia）的中间过渡文件
- **注意**：这些文件只是中间产物，**没有被 `3.py` 直接使用**（`3.py` 使用的是深度清洗后的 `Deep_Cleaned_Chinese`）
- 生成方式：先手动清洗（保留正文，删除致谢等无关内容），然后运行 `2.py` 脚本生成

**文件列表**（部分示例）：
1. `clean_A Will Eternal.txt` - 《一念永恒》（翻译版）
2. `clean_Battle Through the Heavens.txt` - 《斗破苍穹》（翻译版）
3. `clean_Coiling Dragon.txt` - 《盘龙》（翻译版）
4. `clean_Cultivation Chat Group.txt` - 《修真聊天群》（翻译版）
5. `clean_Desolate Era.txt` - 《莽荒纪》（翻译版）
6. `clean_Forty Millenniums of Cultivation.txt` - 《修真四万年》（翻译版）
7. `clean_I Shall Seal the Heavens.txt` - 《我欲封天》（翻译版）
8. `clean_Legendary Mechanic.txt` - 《传奇机械师》（翻译版）
9. `clean_Library of Heaven is Path.txt` - 《天道图书馆》（翻译版）
10. `clean_Lord of the Mysteries.txt` - 《诡秘之主》（翻译版）
...（共20本）

**文件格式**：
- 纯文本文件（.txt）
- UTF-8编码
- 段落之间用两个换行符分隔（`\n\n`）

**数据特点**：
- 已移除所有翻译人员名单（如"Translator: xxx"）
- 已移除网站水印（如"NovelFull"、"read on webnovel"等）
- 已移除URL链接和求打赏信息
- 已移除纯分割线（如"***"、"---"等）
- 已移除重复的章节标题
- 保留完整的正文内容

**文件大小**：
- 单个文件大小：从几MB到几十MB不等
- 总大小：约500-800MB（因书籍数量增加）


**重要注意事项**：
- **手动清洗流程**：必须**先进行手动清洗**，然后再运行 `2.py` 脚本。
- **禁止操作**：不要先运行脚本再手动清洗输出文件。
- **覆盖风险**：如果在手动清洗后不小心再次执行了 `python 2.py`，输出文件夹（`Cleaned_Chinese_Trans_V3`）中的内容会被覆盖，**辛苦手动清洗的内容将会丢失**（如果手动清洗是在输出文件夹中进行的）。
- 建议：手动清洗应针对源文件进行，或者在运行脚本前备份手动清洗后的文件。

---

#### 5.1.2 `Cleaned_English_V2/` 目录

**文件路径**：`/root/autodl-tmp/clean/Cleaned_English_V2/`

**目录用途**：
- 存储西方科幻（Western_SciFi）的中间过渡文件
- **注意**：这些文件只是中间产物，**没有被 `3.py` 直接使用**（`3.py` 使用的是深度清洗后的 `Deep_Cleaned_English`）
- 生成方式：先手动清洗（保留正文，删除致谢等无关内容），然后运行 `1.py` 脚本生成

**文件列表**（部分示例）：
1. `clean_253.txt` - 《253》（Geoff Ryman）
2. `clean_A Clash of Kings.txt` - 《列王的纷争》（George R.R. Martin）
3. `clean_Anansi Boys.txt` - 《阿南西之子》（Neil Gaiman）
4. `clean_Babel.txt` - 《通天塔》（R.F. Kuang）
5. `clean_Dune.txt` - 《沙丘》（Frank Herbert）
6. `clean_Foundation Trilogy.txt` - 《基地三部曲》（Isaac Asimov）
7. `clean_Game of Thrones.txt` - 《权力的游戏》（George R.R. Martin）
8. `clean_Harry Potter.txt` - 《哈利波特》（J.K. Rowling）
9. `clean_Neuromancer.txt` - 《神经漫游者》（William Gibson）
10. `clean_The Fifth Season.txt` - 《第五季》（N.K. Jemisin）
...（共200本，涵盖经典科幻、奇幻、赛博朋克等多种类型）

**文件格式**：
- 纯文本文件（.txt）
- UTF-8编码
- 段落之间用两个换行符分隔（`\n\n`）

**数据特点**：
- 已移除所有网站水印（如"NovelFull"、"www.feedbooks.com"等）
- 已移除URL链接
- 已移除目录导航内容
- 已移除长句高频重复内容
- 已移除版权声明（部分）
- 保留完整的正文内容

**文件大小**：
- 单个文件大小：从几MB到几十MB不等
- 总大小：约2-4GB（因书籍数量大幅增加）


**重要注意事项**：
- **手动清洗流程**：必须**先进行手动清洗**，然后再运行 `1.py` 脚本。
- **禁止操作**：不要先运行脚本再手动清洗输出文件。
- **覆盖风险**：如果在手动清洗后不小心再次执行了 `python 1.py`，输出文件夹（`Cleaned_English_V2`）中的内容会被覆盖，**辛苦手动清洗的内容将会丢失**（如果手动清洗是在输出文件夹中进行的）。
- 建议：手动清洗应针对源文件进行，或者在运行脚本前备份手动清洗后的文件。

---

### 5.2 原始未清洗文本文件（可选参考）

**文件路径**：
- `/root/autodl-tmp/clean/英文中方/` - 原始中方网文文件（未清洗）
- `/root/autodl-tmp/clean/英文西方/` - 原始西方科幻文件（未清洗）

**目录用途**：
- 存储原始下载的文本文件（未经过清洗处理）
- 作为数据清洗前的备份
- 用于对比清洗前后的差异

**文件特点**：
- 包含原始文本的所有内容
- 可能包含翻译人员名单、网站水印、URL链接等垃圾内容
- 格式可能不统一
- 编码可能不一致（UTF-8或Latin-1）

**注意事项**：
- 这些文件仅作为备份和参考
- 实际训练和分析使用的是深度清洗后的文件（`Deep_Cleaned_Chinese/` 和 `Deep_Cleaned_English/`）
- 不建议直接使用这些文件进行训练

---

### 5.3 深度清洗后的文本文件

#### 5.3.1 `Deep_Cleaned_Chinese/` 目录

**文件路径**：`/root/autodl-tmp/clean/Deep_Cleaned_Chinese/`

**目录用途**：
- 存储经过深度清洗处理后的中方网文文本文件
- 在 `Cleaned_Chinese_Trans_V3/` 基础上进行了更细致的清洗
- 包含20本中方网文小说

**文件格式**：
- 纯文本文件（.txt）
- UTF-8编码
- 文件命名与 `Cleaned_Chinese_Trans_V3/` 一致

**数据特点**：
- 基于 `deep_clean_report.csv` 中记录的问题进行了深度清洗
- 进一步移除了残留的符号分割线、水印等内容
- 文件数量：20个

---

#### 5.3.2 `Deep_Cleaned_English/` 目录

**文件路径**：`/root/autodl-tmp/clean/Deep_Cleaned_English/`

**目录用途**：
- 存储经过深度清洗处理后的西方科幻文本文件
- 在 `Cleaned_English_V2/` 基础上进行了更细致的清洗
- 包含200本西方科幻小说

**文件格式**：
- 纯文本文件（.txt）
- UTF-8编码
- 文件命名与 `Cleaned_English_V2/` 一致

**数据特点**：
- 基于 `deep_clean_report.csv` 中记录的问题进行了深度清洗
- 进一步移除了残留的符号分割线、水印等内容
- 文件数量：200个

**注意事项**：
- `Deep_Cleaned_*` 目录是最终使用的清洗版本
- 训练数据集 `bert_training_dataset.csv` 基于这些深度清洗后的文件生成

---

### 5.4 时间戳参考文件

#### `获奖(完结)时间/` 目录

**文件路径**：`/root/autodl-tmp/clean/获奖(完结)时间/`

**目录用途**：
- 存储各类奖项获奖时间和中文小说完结时间的参考资料
- 用于为训练数据中的 `Timestamps` 字段提供时间戳信息

**文件列表**：
1. `Hugo奖获奖时间.md` - 雨果奖获奖作品时间记录
2. `Locus奖获奖时间.md` - 轨迹奖获奖作品时间记录
3. `Nebula奖获奖时间.md` - 星云奖获奖作品时间记录
4. `菲利普迪克奖获奖时间.md` - 菲利普·迪克奖获奖作品时间记录
5. `中文小说完结时间.md` - 中方网文小说完结时间记录

**文件格式**：Markdown（.md）

**数据说明**：
- 西方科幻作品的时间戳主要基于获奖时间（Hugo、Nebula、Locus、菲利普迪克奖）
- 中方网文作品的时间戳基于小说完结时间
- 这些时间数据被`add_timestamps.py`脚本用于为训练数据添加 `Timestamps` 字段

---

### 5.5 项目脚本和文档文件

以下是项目根目录中的Python脚本和文档文件，用于数据处理、模型训练和分析。

**Python脚本**：

| 文件名 | 用途 | 说明 |
|--------|------|------|
| `1.py` | 数据处理脚本 | 数据预处理相关 |
| `2.py` | 数据处理脚本 | 数据清洗相关 |
| `3.py` | 数据处理脚本 | 数据切分/生成训练集 |
| `4.py` | 辅助脚本 | 辅助数据处理 |
| `add_timestamps.py` | 时间戳添加 | 为训练数据添加`Timestamps`字段，基于`获奖(完结)时间/`中的参考资料 |
| `check_clean.py` | 清洗质量检查 | 检查清洗后文件中是否仍存在垃圾内容，生成`clean_check_report.csv` |
| `check_gpu.py` | GPU检查 | 检查GPU可用性和CUDA环境 |
| `download_model_local.py` | 模型下载 | 下载预训练模型到本地目录 |
| `download_roberta_sentiment_model.py` | 情感模型下载 | 下载RoBERTa情感分析模型到本地 |
| `evaluate_saved_models.py` | 模型评估 | 评估已保存的训练模型 |
| `predict.py` | 预测脚本 | 使用训练好的模型进行文本分类预测 |
| `replot_training_curves.py` | 重绘训练曲线 | 重新绘制训练过程的损失曲线图 |
| `train_bert.py` | BERT训练 | BERT分类模型的主训练脚本 |

**BERTopic分析相关脚本**（位于 `bertopic_analysis/` 目录）：

| 文件名 | 用途 | 说明 |
|--------|------|------|
| `topic_modeling.py` | 主题建模 | BERTopic主题建模的主脚本 |
| `sentiment_analysis.py` | 情感分析 | 对主题文档进行情感得分计算 |
| `visualize_topic_comparison.py` | 可视化对比 | 生成主题来源对比可视化图表 |
| `stopwords.py` | 停用词定义 | 定义BERTopic使用的停用词列表 |

**文档文件**：

| 文件名 | 用途 | 说明 |
|--------|------|------|
| `数据说明文档.md` | 数据文件说明 | 本文档，说明所有数据文件的用途和格式 |
| `详细数据结果报告.md` | 结果报告 | 详细的数据分析和模型训练结果报告 |
| `项目交付文档.md` | 项目交付 | 项目整体交付说明文档 |
| `停用词说明.md` | 停用词说明 | 停用词表的说明文档 |
| `最终错误停用词清单.md` | 错误停用词 | 记录被误标为停用词的词汇清单 |
| `requirements.txt` | 依赖列表 | 项目Python依赖包列表 |
| `bertopic_analysis/requirements_bertopic.txt` | BERTopic依赖 | BERTopic分析模块的依赖包列表 |

---

## 6. 模型配置文件

### 6.1 预训练模型文件

本项目使用了三个预训练的深度学习模型，这些模型文件存储在本地目录中，避免训练和推理时从网络下载。

---

#### 6.1.1 BERT分类模型：`bert-base-uncased-local/`

**文件路径**：`/root/autodl-tmp/clean/bert-base-uncased-local/`

**模型用途**：
- 用于文本分类任务（区分Western_SciFi和Chinese_Xianxia）
- 作为BERT分类模型的基础预训练模型
- 在训练过程中进行微调（Fine-tuning）

**模型信息**：
- **模型名称**：BERT-base-uncased
- **模型类型**：双向编码器（Bidirectional Encoder）
- **参数量**：约110M
- **词汇表大小**：30,522
- **最大序列长度**：512 tokens
- **语言**：英文（不区分大小写）

**目录结构**：
```
bert-base-uncased-local/
├── config.json              # 模型配置文件
├── model.safetensors        # 模型权重文件（SafeTensors格式）
├── tokenizer_config.json    # 分词器配置
├── vocab.txt                # 词汇表文件
└── special_tokens_map.json  # 特殊Token映射
```

**文件说明**：

| 文件名 | 说明 | 用途 |
|--------|------|------|
| `config.json` | 模型架构配置 | 定义模型层数、隐藏层大小、注意力头数等 |
| `model.safetensors` | 模型权重 | 包含所有可训练参数（使用SafeTensors格式，更安全） |
| `tokenizer_config.json` | 分词器配置 | 定义如何将文本转换为Token |
| `vocab.txt` | 词汇表 | 包含所有可能的Token及其ID映射 |
| `special_tokens_map.json` | 特殊Token | 定义[CLS]、[SEP]、[PAD]等特殊Token |

**注意事项**：
- 模型文件较大（约400-500MB）
- 使用本地模型可以避免网络下载，提高训练速度
- 模型已针对英文文本进行预训练

---

#### 6.1.2 RoBERTa情感分析模型：`roberta-base-sentiment-local/`

**文件路径**：`/root/autodl-tmp/clean/roberta-base-sentiment-local/`

**模型用途**：
- 用于情感分析任务
- 计算文本的情感得分（-1到1之间）
- 在主题分析中用于分析每个主题的情感倾向

**模型信息**：
- **模型名称**：RoBERTa-base-sentiment
- **模型类型**：RoBERTa（Robustly Optimized BERT）
- **参数量**：约125M
- **词汇表大小**：50,265
- **最大序列长度**：512 tokens
- **任务**：情感分析（Sentiment Analysis）

**目录结构**：
```
roberta-base-sentiment-local/
├── config.json              # 模型配置文件
├── model.safetensors        # 模型权重文件
├── tokenizer_config.json    # 分词器配置
├── tokenizer.json           # 分词器文件（JSON格式）
├── vocab.json               # 词汇表（JSON格式）
├── merges.txt               # BPE合并规则
└── special_tokens_map.json   # 特殊Token映射
```

**文件说明**：

| 文件名 | 说明 | 用途 |
|--------|------|------|
| `config.json` | 模型架构配置 | 定义RoBERTa模型的架构参数 |
| `model.safetensors` | 模型权重 | 包含所有可训练参数 |
| `tokenizer.json` | 分词器文件 | 完整的分词器配置（JSON格式） |
| `vocab.json` | 词汇表 | BPE（Byte Pair Encoding）词汇表 |
| `merges.txt` | BPE合并规则 | 定义如何将字符合并为Token |
| `special_tokens_map.json` | 特殊Token映射 | 定义特殊Token的映射关系 |

**情感得分说明**：
- **-1.0**：极端消极情感
- **0.0**：中性情感
- **1.0**：极端积极情感
- 模型输出的是连续值，可以精确反映情感强度


**注意事项**：
- 模型文件较大（约500MB）
- 专门用于情感分析任务
- 在主题分析模块中使用（`sentiment_analysis.py`）

---

#### 6.1.3 Sentence Transformers模型：`all-MiniLM-L6-v2-local/`

**文件路径**：`/root/autodl-tmp/clean/all-MiniLM-L6-v2-local/`

**模型用途**：
- 用于生成文本的向量表示（Embedding）
- 在BERTopic主题建模中用于文档嵌入
- 将文本转换为固定维度的向量（384维）

**模型信息**：
- **模型名称**：all-MiniLM-L6-v2
- **模型类型**：Sentence Transformers（句子嵌入模型）
- **参数量**：约22M（轻量级）
- **输出维度**：384
- **最大序列长度**：256 tokens
- **特点**：轻量级、快速、适合大规模文本处理

**目录结构**：
```
all-MiniLM-L6-v2-local/
├── config.json                      # 模型配置
├── model.safetensors                # 模型权重
├── config_sentence_transformers.json  # Sentence Transformers配置
├── sentence_bert_config.json        # Sentence-BERT配置
├── modules.json                     # 模块配置
├── tokenizer_config.json            # 分词器配置
├── tokenizer.json                   # 分词器文件
├── vocab.txt                        # 词汇表
├── special_tokens_map.json           # 特殊Token映射
├── README.md                        # 模型说明文档
├── 1_Pooling/                       # 池化层配置
│   └── config.json
└── 2_Normalize/                     # 归一化层配置
    └── config.json
```

**文件说明**：

| 文件名 | 说明 | 用途 |
|--------|------|------|
| `config.json` | 基础模型配置 | 定义底层Transformer架构 |
| `model.safetensors` | 模型权重 | 包含所有可训练参数 |
| `config_sentence_transformers.json` | ST配置 | Sentence Transformers框架的配置 |
| `sentence_bert_config.json` | SBERT配置 | Sentence-BERT特定配置 |
| `modules.json` | 模块配置 | 定义模型的模块结构 |
| `tokenizer_config.json` | 分词器配置 | 定义如何将文本转换为Token |
| `tokenizer.json` | 分词器文件 | 完整的分词器配置（JSON格式） |
| `vocab.txt` | 词汇表 | 包含所有可能的Token及其ID映射 |
| `special_tokens_map.json` | 特殊Token | 定义特殊Token的映射 |
| `README.md` | 模型说明 | 模型的详细说明文档 |
| `1_Pooling/config.json` | 池化层配置 | 定义如何从Token嵌入生成句子嵌入 |
| `2_Normalize/config.json` | 归一化层配置 | 定义输出向量的归一化方式 |


**注意事项**：
- 模型文件较小（约80-90MB），适合快速处理
- 专门优化用于生成句子级别的嵌入
- 在BERTopic主题建模中使用（`topic_modeling.py`）

---

### 6.2 训练配置文件

训练配置文件记录了每次模型训练的详细参数设置，便于复现训练结果和对比不同训练配置的效果。

---

#### 6.2.1 `training_config_stage1.json`

**文件路径**：
- `training_output/reports/training_config_stage1.json`

**文件用途**：
- 记录第一阶段训练的配置参数
- 用于复现训练结果
- 便于对比不同训练阶段的配置差异

**文件格式**：JSON

**配置内容示例**：
```json
{
  "model_name": "/root/autodl-tmp/clean/bert-base-uncased-local",
  "batch_size": 16,
  "learning_rate": 2e-05,
  "epochs": 3,
  "max_length": 512,
  "mode": "stage1",
  "downsample": false,
  "use_class_weights": false,
  "use_focal_loss": false,
  "train_samples": 349547,
  "val_samples": 87387,
  "class_names": ["Western_SciFi", "Chinese_Xianxia"],
  "label_map": {
    "Western_SciFi": 0,
    "Chinese_Xianxia": 1
  },
  "timestamp": "2026-02-12 09:00:00"
}
```

**字段说明**：

| 字段名 | 说明 | 示例值 |
|--------|------|--------|
| `model_name` | 预训练模型路径 | `bert-base-uncased-local` |
| `batch_size` | 批次大小 | `16` |
| `learning_rate` | 学习率 | `2e-05` (0.00002) |
| `epochs` | 训练轮数 | `3` |
| `max_length` | 最大序列长度 | `512` tokens |
| `mode` | 训练模式 | `stage1`（第一阶段） |
| `downsample` | 是否下采样 | `false`（不进行下采样） |
| `use_class_weights` | 是否使用类别权重 | `false`（不使用） |
| `use_focal_loss` | 是否使用Focal Loss | `false`（不使用） |
| `train_samples` | 训练样本数 | `349547` |
| `val_samples` | 验证样本数 | `87387` |
| `class_names` | 类别名称列表 | `["Western_SciFi", "Chinese_Xianxia"]` |
| `label_map` | 标签映射 | `{"Western_SciFi": 0, "Chinese_Xianxia": 1}` |
| `timestamp` | 训练时间戳 | `"2026-02-12 09:00:00"` |

**训练特点**：
- 第一阶段：基础训练，不使用类别权重
- 类别不平衡问题可能导致模型偏向多数类（Chinese_Xianxia）

---

#### 6.2.2 `training_config_stage2_classweights.json`

**文件路径**：
- `training_output_stage2_classweights/reports/training_config_stage2_classweights.json`

**文件用途**：
- 记录第二阶段训练的配置参数（使用类别权重）
- 这是**第二阶段模型**的训练配置（不含Focal Loss）
- 用于复现该阶段训练结果

**文件格式**：JSON

**配置内容示例**：
```json
{
  "model_name": "/root/autodl-tmp/clean/bert-base-uncased-local",
  "batch_size": 16,
  "learning_rate": 2e-05,
  "epochs": 3,
  "max_length": 512,
  "mode": "stage2",
  "downsample": false,
  "use_class_weights": true,
  "use_focal_loss": false,
  "train_samples": 349547,
  "val_samples": 87387,
  "class_names": ["Western_SciFi", "Chinese_Xianxia"],
  "label_map": {
    "Western_SciFi": 0,
    "Chinese_Xianxia": 1
  },
  "timestamp": "2026-02-12 09:30:00"
}
```

**字段说明**：
- 与stage1基本相同，主要区别：
  - `mode`: `"stage2"`（第二阶段）
  - `use_class_weights`: `true`（**使用类别权重**）

**训练特点**：
- 第二阶段：使用类别权重平衡类别不平衡问题
- 类别权重计算：根据类别频率自动计算权重
  - Western_SciFi（少数类）：权重较高
  - Chinese_Xianxia（多数类）：权重较低
- **这是高性能模型配置之一**，准确率达99.99%，F1-Score 99.99%（与stage2_focal相当）

---

#### 6.2.3 `training_config_stage2_classweights_focal.json`

**文件路径**：
- `training_output_stage2_classweights_focal/reports/training_config_stage2_classweights_focal.json`

**文件用途**：
- 记录第三阶段训练的配置参数（使用类别权重+Focal Loss）
- 实验性训练配置，用于对比不同损失函数的效果
- 用于复现该训练阶段的结果

**文件格式**：JSON

**配置内容示例**：
```json
{
  "model_name": "bert-base-uncased-local",
  "batch_size": 16,
  "learning_rate": 2e-05,
  "epochs": 3,
  "max_length": 512,
  "mode": "stage2",
  "downsample": false,
  "use_class_weights": true,
  "use_focal_loss": true,
  "train_samples": 349547,
  "val_samples": 87387,
  "class_names": ["Western_SciFi", "Chinese_Xianxia"],
  "label_map": {
    "Western_SciFi": 0,
    "Chinese_Xianxia": 1
  },
  "timestamp": "2026-02-12 09:31:48"
}
```

**字段说明**：
- 与stage2_classweights基本相同，主要区别：
  - `use_focal_loss`: `true`（**使用Focal Loss**）

**训练特点**：
- 第三阶段：同时使用类别权重和Focal Loss
- Focal Loss：专门用于处理类别不平衡问题的损失函数
- 性能指标：准确率99.99%，F1-Score 99.99%（Macro F1 99.99%）
- 与stage2_classweights性能相近，但训练时间可能略有不同

**Focal Loss说明**：
- Focal Loss通过降低易分类样本的权重，让模型更关注难分类样本
- 参数gamma控制难易样本的权重差异
- 通常与类别权重结合使用，效果更佳

---

### 6.3 其他训练输出文件

#### 6.3.1 `error_samples_stage2_classweights_focal.csv`

**文件路径**：
- `training_output_stage2_classweights_focal/reports/error_samples_stage2_classweights_focal.csv`

**文件用途**：
- 记录使用Focal Loss训练的模型预测错误的样本
- 用于分析Focal Loss对错误模式的影响
- 对比不同损失函数的错误样本差异

**文件格式**：CSV

**字段说明**：

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|---------|------|------|
| `text` | String | 文本内容 | "I giggled hysterically..." |
| `true_label` | String | 真实标签 | "Chinese_Xianxia" |
| `predicted_label` | String | 预测标签 | "Western_SciFi" |
| `confidence` | Float | 预测置信度 | 0.7571 |
| `prob_class_0` | Float | 类别0的概率 | 0.7571 |
| `prob_class_1` | Float | 类别1的概率 | 0.2429 |

**数据说明**：
- 仅包含预测错误的样本
- 可以与`error_samples_stage2_classweights.csv`对比，分析Focal Loss的影响
- 错误样本数量通常很少（<10个）

---

#### 6.3.2 `metrics_stage2_classweights_focal.json`

**文件路径**：
- `training_output_stage2_classweights_focal/reports/metrics_stage2_classweights_focal.json`

**文件用途**：
- 记录使用Focal Loss训练的模型的详细评估指标
- 包含每个训练轮次的性能指标
- 用于对比不同损失函数的效果

**文件格式**：JSON

**文件内容结构**：
```json
{
  "overall_accuracy": 0.9998970098527241,
  "roc_auc": 0.9999996783650521,
  "pr_auc": 0.9999998524443298,
  "macro_f1": 0.999880682558935,
  "weighted_f1": 0.9998970113679714,
  "class_0": {
    "precision": 1.0,
    "recall": 0.9998910240110429,
    "f1": 0.9999455090364181
  },
  "class_1": {
    "precision": 1.0,
    "recall": 0.9998997627718935,
    "f1": 0.9999498788739454
  },
  "confusion_matrix": [[27526, 3], [6, 59852]],
  "avg_confidence": 0.9989607334136963,
  "total_training_time": 13424.593098402023,
  "epoch_metrics": [...]
}
```

**字段说明**：
- 与`metrics_stage2_classweights.json`结构相同
- `total_training_time`：总训练时间（秒）
- `epoch_metrics`：每个训练轮次的详细指标数组

**性能对比**：
- **stage2_classweights**：准确率99.99%，F1-Score 99.99%
- **stage2_classweights_focal**：准确率99.99%，F1-Score 99.99%（Macro F1 99.99%）
- 两者性能非常接近，Focal Loss在此数据集上未显著提升性能

---

## 7. 数据文件使用指南

本章节提供项目中所有数据文件的详细字段说明和使用指南，帮助用户快速理解每个文件的结构和用途。

---

### 7.1 训练数据文件字段说明

#### 7.1.1 `bert_training_dataset.csv`

**文件路径**：`/root/autodl-tmp/clean/bert_training_dataset.csv`

**文件用途**：BERT模型训练的主要数据集，包含所有预处理后的文本片段及其标签。

**文件格式**：CSV（逗号分隔值）

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `text` | String | 是 | 文本内容（已清洗的文本片段） | "The starship drifted through the void..." |
| `source` | String | 是 | 文本来源类别 | "Western_SciFi" 或 "Chinese_Xianxia" |
| `book_name` | String | 是 | 来源书籍名称 | "Dune" 或 "Lord of the Mysteries" |
| `chunk_id` | Integer | 是 | 文本片段在书籍中的编号（从1开始） | 1, 2, 3, ... |
| `char_count` | Integer | 是 | 文本字符数（包括空格和标点） | 800-1500（典型值） |
| `estimated_tokens` | Integer | 是 | 估计的Token数量（用于BERT，约4字符=1 token） | 200-400（典型值） |
| `Timestamps` | String | 是 | 文本对应的时间戳（书籍发表/完结/获奖年份） | "2016" |

**数据统计**：
- 总记录数：436,934条
- Western_SciFi（西方科幻）：137,645条（31.5%）
- Chinese_Xianxia（中方网文）：299,289条（68.5%）
- 平均字符数：~1,147字符
- 平均Token数：~287 tokens

---

### 7.2 数据清洗报告文件字段说明

#### 7.2.1 `clean_check_report.csv`

**文件路径**：`/root/autodl-tmp/clean/clean_check_report.csv`

**文件用途**：记录数据清洗过程中检测到的所有垃圾内容，用于质量控制和清洗效果验证。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `文件夹` | String | 是 | 文件所在文件夹路径 | "Cleaned_Chinese_Trans_V3" 或 "Cleaned_English_V2" |
| `文件名` | String | 是 | 源文件名（包含扩展名） | "clean_My House of Horrors.txt" |
| `行号` | Integer | 是 | 垃圾内容所在行号（从1开始） | 1, 2, 3, ... |
| `内容` | String | 是 | 被标记为垃圾的原始内容 | "..." 或 "Translator: xxx" |
| `垃圾类型` | String | 是 | 垃圾内容的分类标签 | "纯分割线"、"翻译人员名单"、"NovelFull水印"、"URL删除"、"其他" |

**垃圾类型详细说明**：
- **纯分割线**：仅包含分隔符的行（如"..."、"---"、"***"等）
- **翻译人员名单**：包含译者、编辑等信息的行（如"Translator: StarveCleric"）
- **NovelFull水印**：网站水印文本（如"read on webnovel"、"NovelFull"等）
- **URL删除**：包含网址的行（如"www.feedbooks.com"）
- **其他**：其他类型的垃圾内容

---

#### 7.2.2 `deleted_report_cn_v3.csv`

**文件路径**：`/root/autodl-tmp/clean/deleted_report_cn_v3.csv`

**文件用途**：记录中方网文（Chinese_Xianxia）清洗过程中删除的内容，详细记录删除原因和位置。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `文件名` | String | 是 | 源文件名（不包含路径） | "Library of Heaven is Path.txt" |
| `行号` | Integer | 是 | 删除内容所在行号（从1开始） | 1, 2, 3, ... |
| `原内容` | String | 是 | 被删除的原始内容（完整行） | "Translator: StarveCleric Editor: Thaddpo" |
| `原因` | String | 是 | 删除原因分类 | "翻译人员名单"、"NovelFull水印"、"URL删除"、"纯分割线"、"其他" |

**删除原因类型说明**：
- **翻译人员名单**：译者、编辑信息（如"Translator: xxx"、"Editor: xxx"）
- **NovelFull水印**：网站水印（如"read on webnovel"、"NovelFull"等）
- **URL删除**：包含网址的内容（如"www.webnovel.com"）
- **纯分割线**：仅分隔符的行（如"..."、"---"等）
- **其他**：其他需要删除的内容

---

#### 7.2.3 `deleted_report_v2.csv`

**文件路径**：`/root/autodl-tmp/clean/deleted_report_v2.csv`

**文件用途**：记录西方科幻（Western_SciFi）清洗过程中删除的内容，详细记录删除原因和位置。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `文件名` | String | 是 | 源文件名（不包含路径） | "Blindsight.txt" |
| `行号` | Integer | 是 | 删除内容所在行号（从1开始） | 1, 2, 3, ... |
| `内容` | String | 是 | 被删除的原始内容（完整行） | "www.feedbooks.com" |
| `原因` | String | 是 | 删除原因分类 | "包含网址 (URL)"、"目录导航"、"长句高频重复"、"其他" |

**删除原因类型说明**：
- **包含网址 (URL)**：网站链接（如"www.feedbooks.com"、"http://..."）
- **目录导航**：目录索引内容（如章节导航、页码等）
- **长句高频重复**：重复出现的句子（如重复26次以上的相同句子）
- **其他**：其他需要删除的内容

---

### 7.3 模型训练输出文件字段说明

#### 7.3.1 模型文件：`best_model_epoch_X.pt`

**文件路径**：
- `training_output/models/best_model_epoch_1.pt(未保存)`
- `training_output/models/best_model_epoch_2.pt(未保存)`
- `training_output/models/best_model_epoch_3.pt(未保存)`
- `training_output_stage2_classweights/models/best_model_epoch_1.pt(未保存)`
- `training_output_stage2_classweights/models/best_model_epoch_2.pt(未保存)`
- `training_output_stage2_classweights/models/best_model_epoch_3.pt`
- `training_output_stage2_classweights_focal/models/best_model_epoch_3.pt`（**最新模型**）

**文件用途**：保存训练好的BERT分类模型，包含模型权重、优化器状态、训练配置等信息。

**文件格式**：PyTorch模型文件（.pt）

**文件内容结构（字典格式）**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `model_state_dict` | Dict | 是 | 模型权重参数（所有层的权重和偏置） | PyTorch state_dict格式 |
| `optimizer_state_dict` | Dict | 是 | 优化器状态（Adam优化器的动量等） | PyTorch optimizer state_dict格式 |
| `epoch` | Integer | 是 | 训练轮数（epoch编号） | 1, 2, 3 |
| `best_f1` | Float | 是 | 最佳F1-Score（验证集上的最高F1分数） | 0.0-1.0（如0.9998） |
| `config` | Dict | 是 | 训练配置参数（模型路径、批次大小、学习率等） | JSON格式的字典 |

**config字段详细说明**：

| 字段名 | 数据类型 | 说明 | 示例值 |
|--------|---------|------|--------|
| `model_name` | String | 预训练模型路径 | "/root/autodl-tmp/clean/bert-base-uncased-local" |
| `batch_size` | Integer | 批次大小 | 16 |
| `learning_rate` | Float | 学习率 | 2e-05 |
| `epochs` | Integer | 总训练轮数 | 3 |
| `max_length` | Integer | 最大序列长度 | 512 |
| `mode` | String | 训练模式 | "stage1" 或 "stage2" |
| `use_class_weights` | Boolean | 是否使用类别权重 | true 或 false |
| `use_focal_loss` | Boolean | 是否使用Focal Loss | true 或 false |
| `train_samples` | Integer | 训练样本数 | 349547 |
| `val_samples` | Integer | 验证样本数 | 87387 |
| `class_names` | List | 类别名称列表 | ["Western_SciFi", "Chinese_Xianxia"] |
| `label_map` | Dict | 标签映射 | {"Western_SciFi": 0, "Chinese_Xianxia": 1} |

**推荐使用**：
- **最佳模型**：`training_output_stage2_classweights_focal/models/best_model_epoch_3.pt`
- **性能指标**：准确率99.99%，F1-Score 99.99%

---

#### 7.3.2 评估报告文件：`classification_report_*.txt`

**文件路径**：
- `training_output/reports/classification_report_best_model_epoch_3.txt`
- `training_output_stage2_classweights/reports/classification_report_best_model_epoch_3.txt`
- `training_output_stage2_classweights_focal/reports/classification_report_stage2_classweights_focal.txt`

**文件用途**：详细的分类性能报告，包含每个类别的精确率、召回率、F1-Score等指标。

**文件格式**：纯文本（TXT）

**文件内容结构**：
```
              Precision    Recall    F1-Score    Support
Western_SciFi        0.9998     0.9999     0.9998     27529
Chinese_Xianxia      0.9999     0.9999     0.9999     59858
准确率 (Accuracy)                             0.9999     87387
```

**字段说明**：

| 字段名 | 数据类型 | 说明 | 取值范围 |
|--------|---------|------|---------|
| `precision` | Float | 精确率：预测为正类的样本中，真正为正类的比例 | 0.0-1.0 |
| `recall` | Float | 召回率：真正为正类的样本中，被正确预测的比例 | 0.0-1.0 |
| `f1-score` | Float | F1分数：精确率和召回率的调和平均 | 0.0-1.0 |
| `support` | Integer | 支持样本数：该类别在验证集中的样本数量 | 正整数 |

---

#### 7.3.3 指标文件：`metrics_*.json`

**文件路径**：
- `training_output/reports/metrics_best_model_epoch_3.json`
- `training_output_stage2_classweights/reports/metrics_stage2_classweights.json`
- `training_output_stage2_classweights/reports/metrics_best_model_epoch_3.json`
- `training_output_stage2_classweights_focal/reports/metrics_stage2_classweights_focal.json`

**文件用途**：以JSON格式保存所有评估指标，便于程序化读取和分析。

**文件格式**：JSON

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `overall_accuracy` | Float | 是 | 总体准确率（所有类别） | 0.0-1.0（如0.9999） |
| `roc_auc` | Float | 是 | ROC曲线下面积 | 0.0-1.0（如1.0000） |
| `pr_auc` | Float | 是 | PR曲线下面积 | 0.0-1.0（如1.0000） |
| `macro_f1` | Float | 是 | 宏平均F1-Score（所有类别F1的平均值） | 0.0-1.0（如0.9998） |
| `weighted_f1` | Float | 是 | 加权平均F1-Score（按样本数加权） | 0.0-1.0（如0.9999） |
| `class_0` | Dict | 是 | Western_SciFi类别的详细指标 | 见下方说明 |
| `class_1` | Dict | 是 | Chinese_Xianxia类别的详细指标 | 见下方说明 |
| `confusion_matrix` | List | 是 | 混淆矩阵（2x2数组） | [[TP, FN], [FP, TN]] |
| `avg_confidence` | Float | 是 | 平均预测置信度 | 0.0-1.0（如0.9990） |
| `val_loss` | Float | 是 | 验证集损失值 | 正数（如0.0005） |
| `total_training_time` | Float | 否 | 总训练时间（秒） | 正数（如13424.59） |
| `epoch_metrics` | List | 否 | 每个训练轮次的详细指标数组 | 数组格式 |

**class_0 和 class_1 字段说明**：

| 字段名 | 数据类型 | 说明 | 示例值 |
|--------|---------|------|--------|
| `precision` | Float | 精确率 | 1.0 |
| `recall` | Float | 召回率 | 0.9992 |
| `f1` | Float | F1-Score | 0.9996 |

**confusion_matrix说明**：
- 格式：`[[TP, FN], [FP, TN]]`
- `TP`（True Positive）：正确预测为正类的数量
- `FN`（False Negative）：错误预测为负类的数量
- `FP`（False Positive）：错误预测为正类的数量
- `TN`（True Negative）：正确预测为负类的数量

---

#### 7.3.4 指标汇总文件：`metrics_summary.json`

**文件路径**：
- `training_output/reports/metrics_summary.json`
- `training_output_stage2_classweights/reports/metrics_summary.json`

**文件用途**：汇总所有训练轮次（epoch）的评估指标，便于对比不同轮次的性能。

**文件格式**：JSON数组

**完整字段说明**（数组中的每个元素）：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `overall_accuracy` | Float | 是 | 总体准确率 | 0.0-1.0（如0.9994） |
| `roc_auc` | Float | 是 | ROC曲线下面积 | 0.0-1.0（如1.0000） |
| `pr_auc` | Float | 否 | PR曲线下面积 | 0.0-1.0 |
| `macro_f1` | Float | 否 | 宏平均F1-Score | 0.0-1.0 |
| `weighted_f1` | Float | 否 | 加权平均F1-Score | 0.0-1.0 |
| `model_path` | String | 是 | 模型文件路径 | "training_output/models/best_model_epoch_1.pt" |
| `epoch` | Integer | 是 | 训练轮数 | 1, 2, 3 |
| `val_loss` | Float | 否 | 验证集损失值 | 正数 |

---

#### 7.3.5 错误样本文件：`error_samples_*.csv`

**文件路径**：
- `training_output_stage2_classweights/reports/error_samples_stage2_classweights.csv`
- `training_output_stage2_classweights_focal/reports/error_samples_stage2_classweights_focal.csv`

**文件用途**：记录模型预测错误的样本，用于分析模型的错误模式和改进方向。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `text` | String | 是 | 文本内容（完整文本片段） | "My case didn't really interest him..." |
| `true_label` | String | 是 | 真实标签（实际类别） | "Western_SciFi" 或 "Chinese_Xianxia" |
| `predicted_label` | String | 是 | 预测标签（模型预测的类别） | "Western_SciFi" 或 "Chinese_Xianxia" |
| `confidence` | Float | 是 | 预测置信度（模型对预测结果的置信程度） | 0.0-1.0（如0.9998） |
| `prob_class_0` | Float | 是 | 类别0（Western_SciFi）的预测概率 | 0.0-1.0（如0.0001） |
| `prob_class_1` | Float | 是 | 类别1（Chinese_Xianxia）的预测概率 | 0.0-1.0（如0.9999） |

**数据说明**：
- 仅包含预测错误的样本
- 置信度通常很高（接近1.0），说明模型对错误预测也很"自信"
- 可用于分析边界案例和模型局限性
- 错误样本数量通常很少（<10个）

---

### 7.4 主题分析数据文件字段说明

#### 7.4.1 `topic_analysis.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis.csv`

**文件用途**：主题建模的主要结果文件，包含每个主题的关键词、频率、分布等信息。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `topic_id` | Integer | 是 | 主题ID（-1表示噪声/未分类，≥0表示有效主题） | -1, 0, 1, 2, ... |
| `topic_keywords` | String | 是 | 主题关键词（逗号分隔的字符串） | "experts, space, skeleton, mental, origin..." |
| `topic_keywords_with_scores` | String | 是 | 带c-TF-IDF分数的关键词（分号分隔） | "experts(0.2247); space(0.2234); skeleton(0.2122)..." |
| `frequency` | Integer | 是 | 该主题的文档数量 | 26444, 26413, ... |
| `percentage` | Float | 是 | 该主题的文档占比（百分比） | 6.05, 6.04, ... |
| `source_distribution` | String | 是 | 来源分布（各类别的文档数，逗号分隔） | "Chinese_Xianxia: 26235, Western_SciFi: 209" |
| `files` | String | 是 | 包含该主题的文件列表（逗号分隔） | "clean_Swallowed Star.txt, clean_Foundation Trilogy.txt..." |

**字段详细说明**：
- **topic_id = -1**：表示噪声主题，包含无法归类到任何主题的文档（通常占比较大）
- **topic_id ≥ 0**：表示有效主题，按频率从高到低排序
- **topic_keywords_with_scores**：格式为"关键词(分数); 关键词(分数); ..."，分数越高表示该词对主题的代表性越强
- **source_distribution**：显示该主题在不同类别中的分布情况，便于分析主题的来源特征

---

#### 7.4.2 `topic_analysis_detailed_topics.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_detailed_topics.csv`

**文件用途**：主题的详细信息，包括代表性文档，用于深入理解每个主题的具体内容。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `Topic` | Integer | 是 | 主题ID | -1, 0, 1, 2, ... |
| `Count` | Integer | 是 | 文档数量（该主题包含的文档数） | 26444, 26413, ... |
| `Name` | String | 是 | 主题名称（基于关键词自动生成） | "0_experts_space_skeleton_mental" |
| `Representation` | String | 是 | 主题表示（关键词列表，JSON数组格式） | "['experts', 'space', 'skeleton', 'mental'...]" |
| `Representative_Docs` | String | 是 | 代表性文档（最能代表该主题的文档片段，JSON数组格式） | "['文档1...', '文档2...', '文档3...']" |

**字段详细说明**：
- **Representative_Docs**：包含最能代表该主题的文档片段（通常是3个），这些文档可以帮助理解主题的实际内容
- **Representation**：主题的关键词列表，以JSON数组格式存储
- **Name**：基于关键词自动生成的主题名称，用于快速识别主题

---

#### 7.4.3 `topic_analysis_document_topics.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_analysis_document_topics.csv`

**文件大小**：~620MB（包含所有文档的完整文本）

**文件用途**：每个文档的主题分配表，记录每个文档被分配到哪个主题，包含文档的完整文本（用于后续情感分析）。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `document_id` | Integer | 是 | 文档ID（从0开始的连续编号） | 0, 1, 2, ... |
| `text_preview` | String | 是 | 文本预览（前200字符，用于快速查看） | "Before they left, Exquisite told..." |
| `text` | String | 是 | 完整文本内容（完整文档文本） | "完整的文档文本..." |
| `topic_id` | Integer | 是 | 分配的主题ID | -1, 0, 1, 2, ... |
| `topic_probability` | Float | 否 | 主题分配概率（文档属于该主题的置信度，可选） | 0.0-1.0（如0.85） |
| `source_file` | String | 是 | 来源文件名 | "clean_Swallowed Star.txt" |
| `source` | String | 否 | 来源类别（可选，如果输入数据包含此字段） | "Chinese_Xianxia" 或 "Western_SciFi" |
| `timestamp` | String | 否 | 时间戳（书籍发表/完结/获奖年份） | "2016" |

**数据说明**：
- 每个文档都会被分配到一个主题（包括噪声主题-1）
- `topic_probability`表示文档属于该主题的置信度（如果HDBSCAN提供了概率信息）
- `text`字段包含完整文本，文件较大
- 这是情感分析的输入文件

**注意事项**：
- 文件非常大（>200MB），包含所有文档的完整文本
- 读取时建议使用`nrows`参数限制行数，或使用分块读取
- 这是情感分析的输入文件

---

#### 7.4.4 `topic_sentiment_final.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/topic_sentiment_final.csv`

**文件用途**：每个主题的情感分析统计结果，结合主题建模和情感分析的综合结果，用于分析不同主题的情感倾向。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `Topic` | Integer | 是 | 主题ID | -1, 0, 1, 2, ... |
| `Sentiment_Mean` | Float | 是 | 平均情感得分（-1到1之间） | -1.0到1.0（如-0.1983） |
| `Sentiment_Std` | Float | 是 | 情感得分标准差（反映情感分布的离散程度） | 0.0-1.0（如0.3378） |
| `Frequency` | Integer | 是 | 该主题的文档数量 | 26444, 26413, ... |
| `Positive_Count` | Integer | 是 | 积极情感文档数（情感得分 > 0） | 6660, ... |
| `Negative_Count` | Integer | 是 | 消极情感文档数（情感得分 < 0） | 19784, ... |
| `Neutral_Count` | Integer | 是 | 中性情感文档数（情感得分 = 0，通常为0） | 0 |
| `Keywords` | String | 是 | 主题关键词标识（用于快速识别主题） | "Topic 0" 或 "experts, space, skeleton" |

**情感得分说明**：
- **Sentiment_Mean > 0**：整体情感倾向为积极
- **Sentiment_Mean < 0**：整体情感倾向为消极
- **Sentiment_Mean ≈ 0**：整体情感倾向为中性
- **Sentiment_Std**：标准差越大，说明该主题内情感分布越分散

**情感分类标准**：
- **Positive_Count**：情感得分 > 0 的文档数
- **Negative_Count**：情感得分 < 0 的文档数
- **Neutral_Count**：情感得分 = 0 的文档数（通常为0）

**数据解读示例**：
- Topic 0: 平均情感-0.1983（略消极），6,660个积极文档，19,784个消极文档
- 说明该主题整体偏向消极情感，但内部有较大差异

---

#### 7.4.5 `temp_sentiment_docs.csv`

**文件路径**：`/root/autodl-tmp/clean/bertopic_analysis/temp_sentiment_docs.csv`

**文件大小**：>200MB（包含所有文档的情感得分）

**文件用途**：文档级情感得分的中间结果文件，记录每个文档的情感得分（-1到1之间），用于生成主题级情感统计。

**文件格式**：CSV

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `document_id` | Integer | 是 | 文档ID（从0开始） | 0, 1, 2, ... |
| `text` | String | 是 | 文档文本（完整文本内容） | "完整的文档文本..." |
| `sentiment_score` | Float | 是 | 情感得分（-1到1之间） | -1.0到1.0（如-0.2345） |
| `topic_id` | Integer | 是 | 主题ID | -1, 0, 1, 2, ... |
| `source_file` | String | 是 | 来源文件名 | "clean_Swallowed Star.txt" |

**情感得分说明**：
- **-1.0**：极端消极情感
- **0.0**：中性情感
- **1.0**：极端积极情感
- 使用RoBERTa-base-sentiment模型计算

**注意事项**：
- 这是中间结果文件，通常不保存（`SAVE_TEMP_FILE = False`）
- **当前项目中此文件不存在**，因为默认配置未启用保存
- 如果需要此文件，可以在运行情感分析时设置 `SAVE_TEMP_FILE = True`
- 文件很大，包含所有文档的完整文本

---

### 7.5 训练配置文件字段说明

#### 7.5.1 `training_config_*.json`

**文件路径**：
- `training_output/reports/training_config_stage1.json`
- `training_output_stage2_classweights/reports/training_config_stage2_classweights.json`
- `training_output_stage2_classweights_focal/reports/training_config_stage2_classweights_focal.json`

**文件用途**：记录每次模型训练的详细参数设置，便于复现训练结果和对比不同训练配置的效果。

**文件格式**：JSON

**完整字段说明**：

| 字段名 | 数据类型 | 是否必需 | 说明 | 取值范围/示例 |
|--------|---------|---------|------|--------------|
| `model_name` | String | 是 | 预训练模型路径（本地或HuggingFace模型ID） | "/root/autodl-tmp/clean/bert-base-uncased-local" |
| `batch_size` | Integer | 是 | 批次大小（每次训练使用的样本数） | 8, 16, 32, 64 |
| `learning_rate` | Float | 是 | 学习率（模型参数更新的步长） | 1e-05到5e-05（如2e-05） |
| `epochs` | Integer | 是 | 训练轮数（完整遍历数据集的次数） | 1, 2, 3, ... |
| `max_length` | Integer | 是 | 最大序列长度（Token数量限制） | 128, 256, 512 |
| `mode` | String | 是 | 训练模式标识 | "stage1" 或 "stage2" |
| `downsample` | Boolean | 是 | 是否下采样（是否对多数类进行下采样以平衡数据） | true 或 false |
| `use_class_weights` | Boolean | 是 | 是否使用类别权重（是否在损失函数中使用类别权重） | true 或 false |
| `use_focal_loss` | Boolean | 是 | 是否使用Focal Loss（是否使用Focal Loss损失函数） | true 或 false |
| `train_samples` | Integer | 是 | 训练样本数（训练集中的样本总数） | 正整数（如349547） |
| `val_samples` | Integer | 是 | 验证样本数（验证集中的样本总数） | 正整数（如87387） |
| `class_names` | List | 是 | 类别名称列表（所有类别的名称） | ["Western_SciFi", "Chinese_Xianxia"] |
| `label_map` | Dict | 是 | 标签映射（类别名称到数字标签的映射） | {"Western_SciFi": 0, "Chinese_Xianxia": 1} |
| `timestamp` | String | 是 | 训练时间戳（训练开始时间） | "2026-02-12 09:31:48" |

**训练模式说明**：
- **stage1**：第一阶段，基础训练，不使用类别权重
- **stage2**：第二阶段，使用类别权重或Focal Loss优化训练

**推荐配置**：
- **最佳模型配置**：`training_config_stage2_classweights_focal.json`
- **特点**：同时使用类别权重和Focal Loss，准确率99.99%，F1-Score 99.99%

---