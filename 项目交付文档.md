# 项目交付文档

##  项目概述

本项目是一个综合性的文本分析系统，包含**有监督分类**和**无监督聚类**两大核心模块。系统能够自动识别和区分**中方网文（仙侠类）**和**西方科幻**两类文本，同时支持无监督主题发现、关键词提取和情感分析。系统采用先进的自然语言处理技术，实现了高精度的分类任务和深度的内容分析。

### 核心功能

#### 有监督分类模块
-  **自动文本分类**：输入任意文本片段，自动判断属于"中方网文"或"西方科幻"
-  **高精度识别**：模型准确率达到99.99%，F1-Score达到99.99%
-  **批量处理**：支持单条文本、批量文件和交互式预测
-  **置信度评估**：提供预测置信度，便于质量评估

#### 无监督聚类模块
-  **主题发现**：基于BERTopic进行无监督主题建模，自动发现文本中的主题模式
-  **c-TF-IDF关键词提取**：使用类级别TF-IDF（c-TF-IDF）算法提取主题关键词并计算重要性分数
-  **主题-情感映射**：计算每个主题的情感加权均分，实现主题与情感的关联分析
-  **可视化分析**：生成主题分布图、情感-主题象限图等多种可视化图表

### 应用场景

- **内容审核与分类**：自动识别文本类别，支持内容审核
- **文本库自动标注**：批量处理文本，自动生成类别标签
- **内容推荐系统**：基于主题和情感进行内容推荐
- **主题发现与分析**：无监督发现文本中的主题模式，分析主题分布和情感倾向
- **学术研究与分析**：支持文本挖掘、主题建模、情感分析等研究任务

---

##  技术方案

### 一、有监督分类模块

#### 模型架构

- **基础模型**：BERT-base-uncased（12层Transformer，110M参数）
- **任务类型**：有监督二分类
- **输入格式**：文本序列（最大512 tokens）
- **输出格式**：类别标签 + 置信度概率

### 技术特点

1. **数据不平衡处理**
   - 采用类别加权损失函数（Class Weighted Loss）
   - 自动平衡多数类和少数类的学习权重
   - 确保两个类别都能获得良好的分类效果

2. **分层采样策略**
   - 训练集和验证集采用分层采样（Stratified Sampling）
   - 保持训练/验证集的类别分布一致
   - 确保评估结果的可靠性

3. **完整的评估体系**
   - 多维度指标：准确率、精确率、召回率、F1-Score
   - ROC曲线和PR曲线分析
   - 混淆矩阵可视化
   - 错误样本分析

### 二、无监督聚类模块

#### 技术架构

- **主题建模框架**：BERTopic（基于BERT嵌入的无监督主题建模）
- **嵌入模型**：SentenceTransformer (all-MiniLM-L6-v2)
- **聚类算法**：UMAP降维 + HDBSCAN聚类
- **关键词提取**：c-TF-IDF（类级别TF-IDF）
- **情感分析模型**：RoBERTa-base-sentiment

#### 核心算法

1. **无监督主题聚类**
   - **文本嵌入**：使用SentenceTransformer将文本转换为高维向量表示
   - **降维处理**：使用UMAP将高维嵌入降维到低维空间（默认2维）
   - **密度聚类**：使用HDBSCAN进行密度聚类，自动发现主题簇
   - **噪声处理**：无法归类的文档标记为噪声（topic_id = -1）

2. **c-TF-IDF关键词提取**
   - **算法原理**：c-TF-IDF（class-based TF-IDF）是TF-IDF的变体，专门用于主题建模
   - **计算公式**：
     ```
     c-TF-IDF(t, k) = TF(t, k) × log(1 + N / DF(t))
     ```
     其中：
     - `TF(t, k)`：词t在主题k中的词频
     - `N`：主题总数
     - `DF(t)`：包含词t的主题数
   - **优势**：相比传统TF-IDF，c-TF-IDF能够更好地识别主题特有的关键词
   - **输出**：每个主题的关键词列表及其c-TF-IDF分数，分数越高表示该词对主题越重要

3. **主题-情感加权映射**
   - **情感计算**：使用RoBERTa-base-sentiment模型计算每段文本的情感极性分
     - 情感得分范围：[-1, 1]
     - -1：极负面，0：中性，1：极正面
     - 计算公式：`sentiment_polarity = Prob(Positive) - Prob(Negative)`
   
   - **加权平均计算**：
     - 如果存在主题概率（topic_probability），使用加权平均：
       ```
       E_k = Σ(p_i × S_i) / Σ(p_i)
       ```
       其中：
       - `E_k`：主题k的情感均分
       - `p_i`：文档i属于主题k的概率
       - `S_i`：文档i的情感得分
     
     - 如果没有主题概率，使用简单平均：
       ```
       E_k = Mean(S_i)
       ```
   
   - **输出指标**：
     - `Sentiment_Mean`：主题情感均分
     - `Sentiment_Std`：情感得分标准差
     - `Frequency`：该主题的文档数量
     - `Positive_Count`：正面文本数
     - `Negative_Count`：负面文本数
     - `Neutral_Count`：中性文本数

#### 技术特点

1. **无需标注数据**
   - 完全无监督，不需要预先标注的训练数据
   - 自动发现数据中的主题模式

2. **可解释性强**
   - 每个主题提供关键词列表和c-TF-IDF分数
   - 可视化展示主题分布和关键词重要性

3. **支持多维度分析**
   - 主题频率统计
   - 主题来源分布（中方/西方对比）
   - 主题情感倾向分析
   - 主题-情感象限图可视化

4. **灵活的停用词处理**
   - 内置200+停用词（包括专有名词、常见功能词等）
   - 支持自定义停用词列表
   - 通过CountVectorizer实现停用词过滤

#### 性能指标

基于436,934个文档的主题建模结果：

- **发现主题数**：158个有效主题
- **有效主题文档数**：381,245（87.25%）
- **噪声文档数**：55,689（12.75%）
- **主题覆盖率**：前5个主题占约26.50%的文档
- **主题区分度**：能够清晰区分不同类型的内容（军事、炼金术、农业、科技、女巫、龙族等）

**主题分布特点**：
- 分布更均匀：前5个主题占约26.50%的文档
  - Topic 0（专家/太空/骷髅）：26,444个文档（6.05%）
  - Topic 1（思考/毒药/画作）：26,413个文档（6.05%）
  - Topic 2（炼化/天道/命运）：23,117个文档（5.29%）
  - Topic 3（司徒/咒语/杀意）：21,500个文档（4.92%）
  - Topic 4（神秘/附身/毒药）：18,292个文档（4.19%）
- 长尾分布：大部分主题占比很小（<1%），共158个有效主题
- 来源多样：更新后的200本西方科幻使主题来源更加多元化

**c-TF-IDF关键词质量**：
- 语义相关性好：主题关键词具有明显的语义相关性
  - 例如Topic 7（医药主题）：medicine(0.3165), scarlet(0.3067), lychee(0.2528)
  - 例如Topic 8（角色/剧情主题）：character(0.3418), storyline(0.3203), intelligence(0.2319)
- 区分度高：不同主题的关键词能够有效区分
  - 医药主题（Topic 7）：medicine, scarlet, lychee, daoist
  - 犯罪心理主题（Topic 22）：homicide, psychology, victims
  - 交通旅行主题（Topic 20）：car, train, drove
- 可解释性强：每个主题的关键词能够清晰表达主题含义，c-TF-IDF分数直观反映关键词重要性

**主题-情感映射结果**：
- 情感得分范围：[-1, 1]，已为158个主题计算情感加权均分
- 情感分布：所有158个主题情感均为负面（符合小说冲突情节特点）
- 情感范围：从-0.439（Topic 137）到-0.030（Topic 88）
- 负面主题示例：Topic 137（最负面，-0.439）、Topic 44（媒体/监察，-0.388）、Topic 26（-0.379）

---

##  数据集情况

### 数据来源

- **中方网文（Chinese_Xianxia）**：20本中方仙侠/修炼类小说
- **西方科幻（Western_SciFi）**：200本西方经典科幻小说

### 数据统计

| 类别 | 样本数量 | 占比 | 平均字符数 | 平均Token数 |
|------|---------|------|-----------|------------|
| Chinese_Xianxia | 299,289 | 68.5% | ~1,146 | ~287 |
| Western_SciFi | 137,645 | 31.5% | ~1,148 | ~287 |
| **总计** | **436,934** | **100%** | - | - |

**类别比例**：2.17:1（中度不平衡）

### 数据预处理

1. **文本清洗**
   - 去除特殊字符和格式标记
   - 统一换行符处理
   - 保留核心文本内容

2. **文本切分**
   - 滑动窗口切分（窗口大小：1200字符，重叠：150字符）
   - 确保每个片段在BERT的512 token限制内
   - 最小片段长度：200字符

3. **数据平衡**
   - 训练阶段使用类别加权损失函数
   - 保留所有数据，不丢失信息
   - 通过权重自动平衡学习过程

---

##  模型性能

### 最终模型指标（阶段2 - 类别加权 + Focal Loss）

基于验证集（87,387个样本）的评估结果：

#### 总体性能

| 指标 | 数值 |
|------|------|
| **总体准确率** | **99.99%** |
| **宏平均F1-Score** | **99.99%** |
| **加权平均F1-Score** | **99.99%** |
| **ROC-AUC** | **1.0000** |
| **PR-AUC** | **1.0000** |

#### 各类别详细指标

**Western_SciFi（西方科幻）**
- 精确率（Precision）：**100.00%**
- 召回率（Recall）：**99.99%**
- F1-Score：**99.99%**
- 支持样本数：27,529

**Chinese_Xianxia（中方网文）**
- 精确率（Precision）：**100.00%**
- 召回率（Recall）：**99.99%**
- F1-Score：**99.99%**
- 支持样本数：59,858

#### 混淆矩阵

| 真实\\预测 | Western_SciFi | Chinese_Xianxia |
|----------|--------------|----------------|
| Western_SciFi | 27,526 | 3 (0.01%) |
| Chinese_Xianxia | 6 (0.01%) | 59,852 |

**关键观察**：
- 3个西方科幻样本被误判为中方网文，6个中方网文被误判为西方科幻
- 总误判数9个（误判率仅0.0103%）
- F1-Score差异：0.0000（远低于0.1的理想阈值）

#### 预测置信度

- **平均置信度**：99.90%
- **低置信度样本（<0.7）**：0个（0.00%）
- **高置信度样本（≥0.9）**：87,382个（99.99%）

### 训练配置

- **模型**：BERT-base-uncased（本地版本）
- **批次大小**：16
- **学习率**：2e-5
- **训练轮数**：3 epochs
- **训练样本**：349,547
- **验证样本**：87,387
- **训练模式**：阶段2（类别加权 + Focal Loss）

---

##  项目文件结构

```
clean/
├── 1.py                              # 脚本文件
├── 2.py                              # 脚本文件
├── 3.py                              # 数据切分/生成训练集
├── 4.py                              # 脚本文件
├── Cleaned_Chinese_Trans_V3/         # 初步清洗后的中方网文文本（中间产物）
├── Cleaned_English_V2/               # 初步清洗后的西方科幻文本（中间产物）
├── Deep_Cleaned_Chinese/             # 深度清洗后的中文文本
├── Deep_Cleaned_English/             # 深度清洗后的英文文本
├── __pycache__/                      # Python 编译缓存目录
├── add_timestamps.py                 # 时间戳处理脚本
├── all-MiniLM-L6-v2-local/           # 本地 SentenceTransformer 模型
├── bert-base-uncased-local/          # 本地 BERT-base 模型
├── bert_training_dataset.csv         # 有监督分类训练数据集
├── bertopic_analysis/                # 无监督 BERTopic 分析模块
│   ├── __pycache__/
│   ├── requirements_bertopic.txt     # BERTopic 模块依赖列表
│   ├── sentiment_analysis.py         # 主题情感分析脚本
│   ├── sentiment_quadrant_chart.html # 情感-主题象限图
│   ├── stopwords.py                  # 停用词配置
│   ├── topic_analysis.csv            # 主题聚类汇总结果
│   ├── topic_analysis_detailed_topics.csv
│   ├── topic_analysis_document_topics.csv
│   ├── topic_analysis_output/        # 主题可视化输出目录
│   ├── topic_modeling.py             # BERTopic 主题建模脚本
│   ├── topic_sentiment_final.csv     # 主题情感最终统计表
│   └── visualize_topic_comparison.py # 主题对比与可视化脚本
├── check_clean.py                    # 数据清洗检查工具
├── check_gpu.py                      # GPU 环境检查工具
├── clean_check_report.csv            # 数据清洗检查报告
├── deep_clean_report.csv             # 深度清洗报告
├── deleted_report_cn_v3.csv          # 中文删除记录报告
├── deleted_report_v2.csv             # 英文删除记录报告
├── download_model_local.py           # 下载 / 准备本地模型脚本
├── download_roberta_sentiment_model.py # 下载 RoBERTa 情感模型脚本
├── evaluate_saved_models.py          # 已保存模型评估脚本
├── predict.py                        # 预测脚本（单条 / 批量 / 交互）
├── replot_training_curves.py         # 训练曲线重新绘制脚本
├── requirements.txt                  # 主依赖列表
├── roberta-base-sentiment-local/     # 本地 RoBERTa 情感模型
├── train_bert.py                     # BERT 训练脚本
├── training_output_stage2_classweights_focal/  # 阶段2 训练输出目录
│   ├── models/
│   │   └── best_model_epoch_3.pt
│   └── reports/
│       ├── classification_report_stage2_classweights_focal.txt
│       ├── confusion_matrix_stage2_classweights_focal.png
│       ├── roc_pr_curves_stage2_classweights_focal.png
│       ├── training_curves_stage2_classweights_focal.png
│       ├── metrics_stage2_classweights_focal.json
│       └── training_config_stage2_classweights_focal.json
├── 停用词说明.md                      # 停用词相关说明文档
├── 数据说明文档.md                    # 数据集详细说明
├── 最终错误停用词清单.md              # 停用词错误清单最终版
├── 英文中方/                          # 英文语料（中方）
├── 英文西方/                          # 英文语料（西方）
├── 获奖(完结)时间/                    # 获奖 / 完结时间相关数据
├── 详细数据结果报告.md                # 详细数据结果分析报告
└── 项目交付文档.md                    # 本交付说明文档
```

---

##  使用指南

### 环境要求

- **Python版本**：3.10 及以上（推荐 3.10–3.12）
- **操作系统**：Linux / Windows / macOS
- **GPU**：推荐使用GPU加速（CUDA支持），CPU也可运行但速度很慢（建议选RTX 4090D）
- **显存**：建议选用24GB

### 安装步骤

1. **安装Python依赖**

```bash
pip install -r requirements.txt
```

依赖包列表（主环境，见 `requirements.txt`）：
- torch >= 2.0.0
- transformers >= 4.41.0, < 6.0.0
- pandas >= 1.3.0
- numpy >= 1.21.0
- scikit-learn >= 1.0.0
- matplotlib >= 3.4.0
- seaborn >= 0.11.0
- tqdm >= 4.62.0
 - bertopic >= 0.15.0
 - sentence-transformers >= 5.0.0
 - plotly >= 5.0.0

无监督聚类模块如需在**独立环境**中运行，可参考 `bertopic_analysis/requirements_bertopic.txt`，其中额外列出了：
- umap-learn >= 0.5.0
- hdbscan >= 0.8.0
- scipy >= 1.7.0

2. **准备BERT模型**

项目已包含本地BERT模型文件（`bert-base-uncased-local/`），无需额外下载。

**重要提示**：
- 确保 `bert-base-uncased-local/` 目录位于项目根目录
- 如果从服务器迁移到本地，模型配置中的路径会自动适配
- 脚本会自动查找本地模型文件，无需手动修改配置

### 使用方法

#### 1. 单条文本预测

```bash
python predict.py --text "Your text here"
```

**示例**：
```bash
python predict.py --text "The starship drifted through the void, its engines silent as the crew prepared for the jump to hyperspace."
```


#### 2. 批量文件预测

```bash
python predict.py --file input.txt --output results.csv
```

`input.txt`格式：每行一条文本

**输出**：`results.csv`包含预测类别和置信度

#### 3. 交互式预测

```bash
python predict.py --interactive
```

进入交互模式，可以连续输入文本进行预测，输入`quit`或`exit`退出。

#### 4. 指定模型路径

```bash
python predict.py --model path/to/model.pt --text "Your text"
```

默认使用最佳模型：`training_output_stage2_classweights_focal/models/best_model_epoch_3.pt`

### 无监督主题建模（新增功能）

#### 1. 主题建模

```bash
cd bertopic_analysis
python topic_modeling.py --input_csv ../bert_training_dataset.csv --output topic_analysis.csv
```

**参数说明**：
- `--input_csv`：输入CSV文件（包含text列）
- `--output`：输出CSV文件路径
- `--num_topics`：指定主题数量（可选，默认自动确定）
- `--min_topic_size`：最小主题大小（默认10）
- `--output_dir`：可视化输出目录（默认topic_analysis_output）

**输出文件**：
- `topic_analysis.csv`：主题频率统计表（包含主题ID、关键词、频率、来源分布等）
- `topic_analysis_detailed_topics.csv`：详细主题信息
- `topic_analysis_document_topics.csv`：每个文档的主题分配
- `topic_analysis_output/`：可视化图表（HTML格式）

#### 2. 主题-情感映射分析

```bash
cd bertopic_analysis
python sentiment_analysis.py
```

**功能**：
- 读取 `topic_analysis_document_topics.csv`
- 为每个文档计算情感得分
- 计算每个主题的情感加权均分
- 生成情感-主题象限图

**输出文件**：
- `topic_sentiment_final.csv`：主题情感统计表
- `sentiment_quadrant_chart.html`：交互式情感-主题象限图

**象限图说明**：
- X轴：主题热度（文档数量）
- Y轴：主题情感均分（-1到1）
- 点的大小：代表主题频率
- 点的颜色：代表情感倾向（红色=正面，蓝色=负面）

### 训练新模型（可选）

如果需要重新训练或调整模型：

#### 阶段1：快速验证（下采样）

```bash
python train_bert.py --mode stage1
```

- 使用下采样平衡数据
- 快速验证模型可行性
- 适合快速测试

#### 阶段2：优化模型（类别加权）

```bash
python train_bert.py --mode stage2
```

- 使用类别加权损失函数
- 保留所有数据
- 获得最佳性能（推荐）

#### 自定义参数

```bash
python train_bert.py --mode stage2 \
    --batch_size 32 \
    --epochs 5 \
    --lr 3e-5
```

---

##  技术细节

### 无监督聚类技术细节

#### c-TF-IDF算法详解

c-TF-IDF（class-based TF-IDF）是BERTopic的核心算法，用于提取主题关键词：

1. **与传统TF-IDF的区别**：
   - 传统TF-IDF：计算词在整个文档集合中的重要性
   - c-TF-IDF：计算词在特定主题（类）中的重要性
   - 优势：能够更好地识别主题特有的关键词，减少通用词的干扰

2. **计算流程**：
   ```
   对于每个主题k：
     1. 将属于主题k的所有文档合并为一个"类文档"
     2. 计算每个词在该类文档中的词频（TF）
     3. 计算每个词的主题分布频率（DF）
     4. 计算c-TF-IDF分数 = TF × log(1 + N/DF)
   ```

3. **关键词排序**：
   - 按c-TF-IDF分数降序排列
   - 分数越高，表示该词对主题越重要
   - 通常取前10-15个词作为主题关键词

#### 主题-情感加权映射计算

主题情感均分的计算考虑了文档属于主题的概率：

1. **加权平均公式**（推荐）：
   ```
   E_k = Σ(p_i × S_i) / Σ(p_i)
   ```
   其中：
   - `E_k`：主题k的情感均分
   - `p_i`：文档i属于主题k的概率（从HDBSCAN聚类获得）
   - `S_i`：文档i的情感得分（-1到1之间）

2. **简单平均公式**（当无概率信息时）：
   ```
   E_k = Mean(S_i)
   ```

3. **优势**：
   - 加权平均能够更好地反映主题的真实情感倾向
   - 高概率文档对主题情感的影响更大
   - 减少噪声文档对结果的影响

#### 聚类参数调优

1. **min_topic_size**（最小主题大小）：
   - 默认值：10
   - 作用：控制主题的最小文档数
   - 调优建议：
     - 数据量大：可适当增大（15-20）
     - 数据量小：可适当减小（5-8）
     - 噪声文档过多：减小此值

2. **nr_topics**（主题数量）：
   - 默认值：None（自动确定）
   - 作用：限制主题数量，合并相似主题
   - 调优建议：
     - 主题过多：设置20-30
     - 主题过少：不设置，让算法自动发现

3. **embedding_model**（嵌入模型）：
   - 推荐：all-MiniLM-L6-v2（速度快，效果好）
   - 备选：BERT-base-uncased（更准确但更慢）
   - 选择依据：数据量、计算资源、精度要求

### 有监督分类技术细节

### 数据不平衡处理策略

项目采用**类别加权损失函数**（Class Weighted Loss）处理数据不平衡问题：

1. **自动计算权重**
   - 根据训练集中各类别的样本数量自动计算权重
   - 少数类（Western_SciFi）获得更高权重（约1.59）
   - 多数类（Chinese_Xianxia）权重约0.73

2. **优势**
   - 保留所有数据，不丢失信息
   - 通过损失权重自动平衡学习过程
   - 适合生产环境，模型能学习到更多特征

3. **效果验证**
   - 两个类别的F1-Score接近（差异仅0.0000）
   - 少数类召回率达到99.99%
   - 总体准确率99.99%

### 模型训练流程

1. **数据加载**：从CSV文件加载预处理好的数据
2. **数据划分**：80%训练集，20%验证集（分层采样）
3. **模型初始化**：加载BERT-base预训练模型
4. **训练循环**：
   - 每个epoch记录训练损失和验证损失
   - 计算F1-Score、精确率、召回率等指标
   - 保存最佳模型（基于验证损失和F1-Score）
5. **评估与报告**：
   - 生成详细的分类报告
   - 绘制混淆矩阵、ROC曲线、PR曲线
   - 保存错误样本分析

### 评估指标说明

- **准确率（Accuracy）**：正确预测的样本比例
- **精确率（Precision）**：预测为正类的样本中，真正为正类的比例
- **召回率（Recall）**：真正为正类的样本中，被正确预测的比例
- **F1-Score**：精确率和召回率的调和平均，综合指标
- **ROC-AUC**：ROC曲线下面积，衡量分类器整体性能
- **PR-AUC**：精确率-召回率曲线下面积，对不平衡数据更有意义

---

##  性能分析

### 训练曲线分析

模型训练过程稳定，各项指标持续改善：

1. **损失曲线**：训练损失和验证损失均持续下降，无过拟合迹象
2. **F1-Score曲线**：两个类别的F1-Score在训练过程中逐渐接近
3. **类别平衡**：F1差异降至0.0000，达到理想状态

### 错误样本分析

- **错误样本总数**：9个（仅占验证集的0.0103%）
- **错误类型**：3个Western_SciFi被误判为Chinese_Xianxia，6个Chinese_Xianxia被误判为Western_SciFi
- **可能原因**：文本特征边界模糊，或包含混合风格内容

### 模型优势

1. **高精度**：99.99%的准确率，满足生产环境要求
2. **类别平衡**：两个类别F1-Score接近，无偏向性
3. **高置信度**：99.99%的样本置信度≥0.9，预测可靠
4. **泛化能力强**：在验证集上表现优异，具备良好的泛化能力

---

##  GPU使用指南

### 运行环境（AutoDL云端GPU服务器）

- **镜像环境**：
  - PyTorch 2.5.1
  - Python 3.12 (ubuntu22.04)
  - CUDA 12.4

- **硬件配置**：
  - **内存**：80GB
  - **硬盘**：
    - 系统盘：30 GB
    - 数据盘 (免费)：50 GB

### 检查GPU使用

运行check_gpu.py

---

##  输出文件说明

### 模型文件

- `best_model_epoch_X.pt`：包含模型状态、优化器状态、epoch信息和训练配置

### 评估报告

- `classification_report_*.txt`：详细的文本分类报告
- `metrics_*.json`：所有指标的JSON格式
- `training_config_*.json`：训练配置参数

### 可视化文件

- `confusion_matrix_*.png`：混淆矩阵（带百分比标注）
- `roc_pr_curves_*.png`：ROC曲线和PR曲线
- `training_curves_*.png`：多维度训练曲线（损失、F1、精确率/召回率、类别平衡）

### 错误分析

- `error_samples_*.csv`：所有错误分类的样本，包含原文、真实标签、预测标签、置信度等

---

##  注意事项

### 数据质量

- 确保输入文本已进行适当的清洗和预处理
- 文本长度建议控制在200-2000字符之间
- 避免包含过多特殊字符或格式标记

### 模型限制

- **最大输入长度**：512 tokens（约2000字符）
- **语言支持**：主要针对英文文本，中文文本需要相应调整
- **类别限制**：当前仅支持二分类（中方网文 vs 西方科幻）

### 性能优化

- 推荐使用GPU加速训练和推理
- 批量预测时，可以调整batch_size以平衡速度和内存
- 对于大规模预测任务，考虑使用多进程或分布式处理

### 模型更新

- 如果数据分布发生变化，建议重新训练模型
- 定期评估模型性能，监控准确率和错误率
- 收集错误样本，用于模型改进

### 本地使用注意事项

**路径问题处理**：
- 如果从服务器迁移到本地，模型配置中可能包含服务器绝对路径
- 脚本会自动尝试多种路径查找BERT模型文件：
  1. 配置中的原始路径
  2. 相对于脚本目录的路径
  3. 项目根目录下的 `bert-base-uncased-local/`
- 确保 `bert-base-uncased-local/` 目录存在于项目根目录
- 如果遇到路径错误，检查目录结构是否正确

**常见错误解决**：
- `OSError: Incorrect path_or_model_id`：确保 `bert-base-uncased-local/` 目录存在
- `FileNotFoundError: 模型文件不存在`：检查模型文件路径是否正确
- 如果问题持续，可以手动指定模型路径：`python predict.py --model <模型路径> --text "..."`

**关于 FutureWarning 警告**：
- 运行时可能看到 `FutureWarning: torch.utils._pytree._register_pytree_node is deprecated` 警告
- **这是正常的**：这是 transformers 库和 PyTorch 版本兼容性提示，不影响功能
- **原因**：transformers 4.36.2 使用了即将被弃用的 PyTorch API，新版本 PyTorch 会提示
- **处理**：代码已自动抑制这些警告，如果仍看到警告，可以忽略，不影响预测结果
- **解决**：未来更新 transformers 库到更新版本即可消除（当前版本已足够稳定）

---

##  项目总结

### 项目成果

#### 有监督分类模块
- **高精度模型**：准确率99.99%，F1-Score 99.99%
- **类别平衡**：两个类别F1-Score差异仅0.0000
- **完整工具链**：从数据预处理到模型训练、评估、预测的完整流程

#### 无监督聚类模块
- **主题发现**：自动发现158个有效主题，覆盖87.25%的文档（381,245个文档）
- **c-TF-IDF关键词提取**：为每个主题提取高相关性关键词，并计算重要性分数
- **主题-情感映射**：实现主题与情感的关联分析，生成情感-主题象限图
- **多维度可视化**：主题分布图、关键词重要性图、情感象限图等

### 技术亮点

#### 有监督分类
1. **数据不平衡处理**：采用类别加权 + Focal Loss，有效解决2.17:1的不平衡问题
2. **完整评估体系**：多维度指标、可视化分析、错误样本分析
3. **生产就绪**：提供完整的预测接口，支持单条、批量、交互式预测
4. **可扩展性**：代码结构清晰，易于扩展和维护

#### 无监督聚类
1. **c-TF-IDF算法**：使用类级别TF-IDF提取主题关键词，比传统TF-IDF更适合主题建模
2. **主题-情感加权映射**：考虑文档属于主题的概率，使用加权平均计算主题情感均分
3. **无监督学习**：无需标注数据，自动发现文本中的主题模式
4. **多维度分析**：支持主题频率、来源分布、情感倾向等多维度分析
5. **丰富的可视化**：交互式图表，支持主题探索和分析

### 交付内容

#### 有监督分类模块
- 训练好的模型文件（BERT分类模型）
- 完整的源代码（训练、预测、评估脚本）
- 详细的评估报告和可视化
- 使用文档和技术文档
- 数据预处理脚本

#### 无监督聚类模块
- 主题建模脚本（topic_modeling.py）
- 情感分析脚本（sentiment_analysis.py）
- 主题分析结果（CSV格式）
- 可视化图表（HTML格式）
- 主题-情感映射结果
- 技术文档和使用说明

---